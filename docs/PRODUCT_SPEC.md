# WPU — 제품 기획 (v0.1 초안, 결정-강제형)

> **성격.** 이 문서는 백지 기획이 아니라 **결정을 강제하는 초안**이다. 각 갈림길에
> **추천 기본값**을 채워 두었으니, 오너는 원점에서 시작하지 말고 **확인/수정**만
> 하면 된다. 근거는 저장소에 이미 있는 것(검증된 RTL·FPGA 핏·프로비저너·API 껍데기·
> 텔레메트리 리드백)과 정직한 제약(실리콘 없음, FPGA는 슬라이스)에 둔다.
> 성능 수치는 전부 **[추정]** — 대역폭 루프라인 투영이며 실리콘 측정이 아니다.

## 0. 결정된 것

- **비치헤드 = 개인 파워유저 (챗 + 코드).** 혼자 쓰는 고성능 로컬 모델 박스.
  에어갭 조직(RAG·GUI·시각화)은 **후속 비치헤드로 연기.** 이 선택의 핵심 이점:
  **RAG·GUI를 지을 필요가 없다** — OpenAI 호환 API로 기존 챗/IDE 클라이언트
  생태계(Open WebUI, Continue.dev 등)를 그대로 붙이면 되므로, v0.1에 필요한
  신규 소프트웨어가 가장 적다.

## 1. v0.1의 정직한 정의 — "경험"과 "하드웨어 증명"의 분리

실리콘이 없고 FPGA는 컴팩트 슬라이스(VOCAB=256)라 **진짜 GLM-5.2 텍스트를 못 낸다.**
그래서 v0.1은 두 갈래를 **분리해서 각각 정직하게** 증명한다:

| 갈래 | 무엇을 증명 | 어떻게 | 지금 가능? |
|---|---|---|---|
| **제품 경험** | 실제 GLM-5.2 텍스트로 챗·코딩이 오프라인에서 된다 | OpenAI 호환 API + **소프트웨어 풀모델 백엔드**(CPU/GPU, 정확하지만 느림), 표준 클라이언트로 | ✅ $0, 지금 |
| **하드웨어** | 우리 데이터패스가 실 칩에 들어가 실 사이클로 돈다 | FPGA 슬라이스(실 P&R 46.5MHz, 토이 어휘) | ✅ 완료 |
| (통합) | 둘이 하나로 — 실리콘 가속 백엔드가 같은 API 뒤로 | 테이프아웃 이후 | ⏸ 실리콘 관문 |

이 분리가 핵심이다: **소프트웨어 백엔드는 "느리지만 진짜 텍스트"라고 명확히 라벨**하고,
FPGA는 "빠르지만 토이"라고 라벨한다. 둘 다 진짜이고, 둘 다 과대표현이 아니다.
설계 파트너에게 "지금 보여줄 것"이 생긴다.

## 2. 결정 목록 (추천 기본값 — 확인/수정 요망)

| # | 결정 | 추천 기본값 | 대안 | 왜 |
|---|---|---|---|---|
| **D1** | 비치헤드 | **개인 파워유저(챗+코드)** — 결정됨 | 조직/데모-우선 | 신규 SW 최소, 생태계 활용 |
| **D2** | v0.1 백엔드 | **소프트웨어 풀모델(경험) + FPGA 슬라이스(HW), 분리** | 실리콘 대기 | 실리콘 없이도 데모 가능 |
| **D3** | 인터페이스 | **데이터 플레인(챗/코드)=생태계 클라이언트(OpenAI API), 컨트롤 플레인(기기 관리)=자체 경량 콘솔** | 챗 GUI도 자체 / 콘솔도 없음 | 챗은 생태계가 이미 함(자체는 헛수고); 헬스·설정·프로비저닝·기능제어는 WPU 고유라 아무도 안 해줌 |
| **D4** | 세션 모델 | **v0.1 단일 컨텍스트 → v0.2 멀티컨텍스트**(방금 만든 ctx-id 프로토콜 + 코어 스케줄러 + 호스트 세션 매니저) | 멀티 지금 | 단순한 것 먼저 출시, 멀티는 오너 요구라 바로 다음 |
| **D5** | 캐싱 | **무조건 프리픽스/KV 캐시**(호스트 프리픽스 캐시 + 턴 간 KV 재사용) | 캐시 없음 | 오너가 "무조건" 요구, 대화 체감 필수 |
| **D6** | 영속성 | **대화 히스토리 → NVMe**(선택적 임베딩), 저장 시 암호화(후속) | 무상태 | "수년치 로그" 비전, 개인 기기 |
| **D7** | 프로비저닝 | **`tools/provision_image.py`로 사용자가 받은 GGUF → NVMe 이미지**(v0.1 소프트웨어는 호스트가 GGUF 직접 읽음) | 공장 사전적재 | 이미 실 GGUF로 증명된 툴 존재 |
| **D8** | 업데이트 | **A/B 듀얼슬롯 + 원자적 활성포인터 + 롤백**(§A 펌웨어) | 단일슬롯 재기록 | 벽돌 방지 — 보드/부트로더 프리즈 전 결정 |
| **D9** | 모델 | **GLM-5.2 기본, 다른 신뢰 GGUF로 재타겟 가능**(박스는 모델-불문 가속기) | 단일 모델 고정 | RTL 작업으로 교체, 하드웨어 불변 |

## 3. v0.1 제품 표면 (개인 파워유저) — 두 층

**데이터 플레인 (대화) = 생태계 클라이언트, 자체 안 지음**
- **챗 + 코드**: OpenAI 호환 `/v1/chat/completions`(이미 `host/wpu_server.py`에 껍데기
  존재) → Open WebUI·Continue.dev·Cursor 등 표준 클라이언트가 바로 붙음.
- **오프라인**: 호스트 로컬에서 완결, 밖으로 나가는 경로 없음.
- **캐싱**: 호스트 프리픽스 캐시 + 턴 간 KV 재사용(무조건).

**컨트롤 플레인 (기기 관리) = 자체 경량 웹 콘솔** — WPU 고유, 생태계가 못 함:
- **헬스 대시보드**: `PROTO_CTX` 텔레메트리 리드백(tok/s·수락률·스톨·전원/발열)을
  실시간 상태로. "잘 돌고 있나"를 한눈에.
- **설정**: 전원/eco 모드, 컨텍스트 수, 캐싱, 로드할 모델 선택.
- **프로비저닝/업데이트**: 올라간 모델·A/B 슬롯 상태·467GB 로딩 진행률.
- **기능 제어 자리(성장)**: RAG 인덱스 관리·문서 추가·기능 토글은 **조직 비치헤드 때**
  이 콘솔이 자라나는 자리로 예약(사이트의 "GUI 튜닝" 비전이 여기로 연결).
- 형태: 호스트 서버(또는 박스)가 `wpu.local` 등에 서빙하는 **작은 상태+설정 화면**
  (풀 챗앱 아님 — 부담 적음).

## 4. 단계별 로드맵

- **Stage 0 — 완료**: 검증된 RTL, FPGA 핏(46.5MHz), 실 프로비저너, 락인 3개 파운데이션
  (부팅 무결성·가중치 ECC·프로토콜 ctx-id), 사이클 측정.
- **Stage 1 — v0.1 (일부 완료 · 2026-07)**: OpenAI API + **소프트웨어 풀모델 백엔드**로
  표준 클라이언트에서 **실제 텍스트**. 두 백엔드:
  **(주) Modal** — `tools/modal_glm_server.py`가 **실제 GLM 패밀리(GLM-4.5-Air-FP8,
  측정에 쓴 모델)**를 vLLM으로 Modal GPU 클라우드(2×H100, 유휴 시 0으로 스케일)에
  서빙, `host/wpu_modal_backend.py`가 프록시. **(대안) llama** — 로컬 작은 GGUF,
  오프라인·무비용. 그리고 **자체 경량 관리 콘솔**(`GET /console`: 헬스/텔레메트리 +
  설정 + `provision_image.py` 매니페스트를 읽는 프로비저닝 패널). 호스트 측 배관
  라이브 검증 완료(Modal 프록시 체인 + llama 실 GGUF). 남은 v0.1: 프리픽스/KV 캐시
  (v0.2). *(소프트웨어 백엔드는 가속기가 아니라 경험 증명 — 실리콘이 같은 API 뒤에서
  이 백엔드를 대체하는 게 Stage 3.)*
- **Stage 2 — 멀티컨텍스트 & 세션 & 확장 자산**: ctx-id 코어 스케줄러 + 호스트 세션
  매니저 + 프리픽스/KV 캐시 + 히스토리 영속성 + **확장 자산 상주·관리**(§8).
- **Stage 3 — 실리콘**: 테이프아웃(진짜 관문) → 실 가속 백엔드가 같은 API 뒤로.
- **Stage 4 — 조직 비치헤드**: RAG + 임베디드 GUI + 시각화(별도 시장 추구 시).
- **스케일아웃 방향(멀티-디바이스)**: §9 — 여러 박스 연결. 로드맵 방향(개인 v0.1 아님).


## 5. 비-목표 (개인 비치헤드 v0.1에서 안 함)

**자체 챗 GUI**(생태계 클라이언트로 대체), RAG, 지식그래프 시각화, 다중 사용자
인증 — 전부 **조직 비치헤드**로 연기. *(단, 자체 **관리 콘솔**은 v0.1에 포함 —
챗 GUI와 다른 것이며, 기기 관리는 생태계가 못 하므로 필수. §3 참조.)*
USB4 전송(실리콘 인터페이스; v0.1 소프트웨어는 로컬 HTTP).

## 6. 이미 깔린 기반 (기획이 공중에 뜨지 않는 이유)

- OpenAI 호환 API 서버 껍데기 + 토크나이저: `host/wpu_server.py`, `host/wpu_tokenizer.py`.
- 실 GGUF → 이미지 프로비저너: `tools/provision_image.py`(실 GGUF 2개로 증명).
- 프로토콜 ctx-id + 텔레메트리 리드백: `glm_q4k_system_cdc.v`(`PROTO_CTX`).
- 부팅 무결성·가중치 ECC 파운데이션: `boot_loader.v`(`INTEGRITY`), `weight_loader_q4k.v`(`WEIGHT_ECC`).
- 갭 추적: [`USAGE_GAPS.md`](USAGE_GAPS.md).

## 7. 오너가 확인할 것

위 D2–D9의 추천 기본값. 특히:
- **D2** — 소프트웨어 백엔드를 "데모"로 받아들이나, 아니면 데모가 반드시 하드웨어에서
  돌아야 하나? (후자면 v0.1이 테이프아웃까지 밀림.)
- **D3** — 챗은 생태계 클라이언트 + 기기 관리는 자체 경량 콘솔, 이 분리에 동의하나?
  (챗 GUI는 안 짓고, 관리 콘솔은 v0.1에 포함.)
- **D7/D8** — 프로비저닝 방식과 A/B 업데이트 정책.

확인/수정 주면 그 결정대로 Stage 1(v0.1) 실제 구현 — 소프트웨어 백엔드를 API에
연결해 표준 클라이언트로 진짜 텍스트가 나오게 — 부터 착수한다.

## 8. 확장 자산 상주·관리 (방향, Stage 2)

개인 파워유저가 박스를 진짜 자기 것으로 쓰려면 **모델뿐 아니라 MCP 서버 정의·
플러그인·툴 설정** 같은 확장 자산도 오프라인에서 딸려와야 한다. 정직한 구분:
- **MCP/플러그인/툴 실행은 클라이언트 쪽 소프트웨어**에서 돈다(생태계 구조). 박스는
  추론을 하지, 툴을 실행하지 않는다.
- 따라서 방향은 **자산의 상주·관리·배포**다(실행 아님): `provision_image.py`가 모델
  이미지 + 매니페스트뿐 아니라 **MCP/플러그인/툴 번들**도 같은 서명 매니페스트
  (sha256·버전)에 담아 NVMe에 상주 → 부팅 무결성 게이트가 모델과 동일하게 커버.
  관리 콘솔에 **확장 패널**(설치 목록·추가/제거·버전·토글), 클라이언트는
  `/api/extensions`로 상주 설정을 받아 자기 MCP 클라이언트에 주입.
- **비-목표**: 박스 안에서 MCP 서버/임의 코드 **실행** — 범용 컴퓨트 노드가 되어야
  하고 보안 조직엔 리스크. 별도 결정으로 연기. *(권고: 상주·관리·배포만.)*

## 9. 스케일아웃 — 여러 박스 연결 (방향 · 현재 비-목표)

> **상태 (2026-07): 현재 작업 대상 아님.** 개인 v0.1엔 이득이 없고(아래), 실물 박스가
> 2대 있어야 진짜 검증되며, 지금 지으면 검증 못 하는 코드의 유지보수 부담만 는다.
> 이 섹션은 **"왜 미루는지"의 근거**로만 남긴다("박스 여러 개 안 이어?"에 대한 답).
> **재개 조건**: 실물 멀티박스를 실제로 추구할 때, 또는 목표 모델이 한 박스 용량
> (~512GB)을 넘을 때. 그 전에 락인해둘 것은 §C 프로토콜에 레이어 범위·히든스테이트
> 필드 자리를 예약하는 것뿐(프로토콜 respin 방지) — 컨트롤러 구현은 그때.

> **핵심 못박기 — 멀티박스는 용량/처리량용, 체감 속도는 아님.** 단일 토큰 속도 =
> `대역폭 ÷ 토큰당 바이트`이고, 박스를 케이블로 이어도 **박스 간 대역폭을 못 합친다**
> (그러려면 NVLink급 링크 = 두 다이를 한 패키지에 붙이는 것 = 이미 한 박스). 그래서:
> **멀티박스는 용량(파이프라인)과 총 처리량(데이터 병렬)을 주고, 단일 토큰 속도는
> 오히려 홉 지연으로 살짝 손해다.** 체감 속도를 올리는 레버는 전부 **박스 안**에 있다
> — 더 넓은 버스(1280-bit→), 더 빠른 LPDDR 빈/세대, 적응형 스펙체인, 실리콘 노드
> (§5c/§7). GPU 개수를 늘려도 안 빨라지던 것(offload 벽)과 같은 원리: 대역폭은 개수가
> 아니라 배치에서 나온다.

박스 간 링크(USB4 ~5GB/s) ≪ 박스 내 메모리(~1.5TB/s). **박스 사이로 흐르는
데이터량**이 어떤 방식이 되는지를 가른다:
- **데이터 병렬 — 지원 방향(처리량).** 박스마다 풀 모델 독립 구동, 호스트 라우터가
  요청 분배. 박스 간 트래픽=요청·응답(수 KB). **N배 총 처리량 / N 사용자·컨텍스트**
  (단일 토큰 속도는 그대로), 메모리-바운드 스레시스와 완벽 부합. 대부분 호스트 라우터
  소프트웨어. 개인·조직 양쪽에 가장 실용적.
- **파이프라인 병렬 — 지원 방향(용량 보험).** 모델을 레이어로 쪼개 박스에 분산.
  박스 간 트래픽=**토큰당 히든스테이트(~KB), 가중치 아님** → USB4로 충분. **한 박스
  용량(512GB) 초과 모델**을 여러 박스로 담는 유일한 저대역폭 경로. **이득은 용량뿐 —
  단일 토큰은 박스들을 순차 통과 + 홉 지연이라 오히려 느려지고, 처리량 이점도 요청이
  겹칠 때만(개인 B=1엔 거의 안 살아난다).** 필요: 프로토콜 확장("레이어 K부터 이
  히든스테이트로 시작" / "레이어 M 이후 방출") + 부팅 파티션(각 박스가 자기 레이어
  범위만 로드 — 프로비저너/세그먼트 디스크립터 확장) + 신규 RTL(레이어-부분 실행
  진입/이탈점 + 박스 간 링크 xbar + 파이프라인 스케줄). 그래서 **차세대 >512GB 모델을
  이 폼팩터로 돌려야 할 때의 보험**이지, 성능 카드가 아니다.
- **텐서 병렬 — 기각.** 각 레이어 연산을 박스에 쪼개면 박스 간 대량 부분합이 필요 →
  테라비트 인터커넥트(NVLink급) 요구, USB4는 굶는다. CXL과 같은 대역폭 벽이라 **근거
  박제 기각**.
- **시기**: 로드맵 방향(설계 의도). 개인 v0.1 아님. 데이터 병렬은 호스트 라우터,
  파이프라인 병렬은 프로토콜/부팅 파티션 확장이 필요.
