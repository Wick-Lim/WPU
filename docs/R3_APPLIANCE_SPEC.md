# rung-③ 어플라이언스 개념 스펙 — 512GB 전체 상주 박스 (v3, 2026-07-10)

**한 줄 요약.** GLM-5.2(UD-Q4_K_XL, ~467GB)를 **통째로 LPDDR5X 512GB에 상주**시키는
단일 사용자 오프라인 박스: 설계점 **≈ 80 tok/s [실측-입력 EST]** (적응형 스펙체인 +
GLM-4.5-Air 실측 U(K)·실측 수락률 r(잡 B) 반영; GLM-5.2의 더 깊은 MTP가 공표
수준이면 ~95 — 이전 표기 ~76–95 대역의 실측 후 갱신), 전력 **≥50–78W [EST] 플로어**
(§4 재도출 — 구 ~40–60W는 폐기, 도출된 적 없음), 보드 120×80mm,
BOM ~$1.8–2.4k. [`HARDWARE_LADDER.md`](HARDWARE_LADDER.md)의 rung-③ 메모리 피벗
(v2)을 실물 스펙으로 전개한 문서. 모든 성능/비용 수치는 **[EST]** — roofline +
h/U 측정(OLMoE 프록시 + GLM-4.5-Air 실측, [`H_MEASUREMENT.md`](H_MEASUREMENT.md))
기반이며, 실리콘/보드
실측이 아니다. 개념 배치도는 세션 아티팩트(요청 시 재생성 가능)로 유지.

## 1. 시스템 구성 (FIXED)

```
[WPU SoC (우리 RTL)] ←1024-bit · ~1.1TB/s→ [LPDDR5X 512GB: 체크포인트 전체 상주]
        ↑ PCIe x4 — 부팅 시에만
[M.2 2230 NVMe 1TB: 콜드 보관] — 전원 인가 후 467GB 로드 ~70초, 이후 관여 없음
        ↑ USB4 (USB-C) — 토큰/프롬프트만. 전력은 별도(아래 §4)
[호스트 PC/폰]
```

- 스트리밍 티어(ONFI 64ch NAND)는 **삭제** — 상주 피벗의 정의. 하이브리드
  1024-bit(~84 tok/s, 단 GLM h≥0.75 전제)는 **같은 다이의 업사이드 SKU**로 보류
  (ONFI 패드는 다이에 유지, 상주 SKU 패키지에서 미배선).
- 컴퓨트 다이 = 지금 검증된 RTL(46.5MHz FPGA에서 1155-test 비트 정확)의 스케일업.
  expert-cache/prefetcher는 상주 구성에서 지연 은닉용으로 강등, weight_loader는
  DRAM 타일 페치로 단순화.

## 2. 성능 산출 (min(NAND, DRAM) 교정 반영) — **토큰당 바이트 상수의 정본(canonical)**

**토큰당 바이트 상수 — 정본 유도 (다른 문서는 이 값을 인용한다; 상이한 수치는
시나리오 라벨 필수):**

- **라우티드 expert 스트림 ≈ 14 GB/token**: 75 MoE층 × top-8 × 37.75M
  params/expert = 22.65B params/token, × **~0.62 B/param** (UD-Q4_K_XL
  467 GB ÷ 753B) ≈ **14 GB**. 스펙체인에서는 검증 패스당 expert *합집합*이
  ×U(K)로 성장하고 수락 A로 상각 → `14·U(K)/A`.
- **핫셋 터치 ≈ 11 GB/token**: 토큰당 활성 ~40B params ≈ ~25 GB에서 라우티드
  14 GB를 뺀 나머지(어텐션 투영 전층 + dense FFN + 공유 expert + router +
  LM-head) — 검증 패스당 1회 읽혀 A로 상각 → `11/A`.
  *(구분: DDR에 **상주해야 하는 핫 파티션 용량**은 비-라우티드 ~28B params ≈
  **~17 GB** (embed/LM-head/norm 포함) — roofline이 나누는 것은 상주 용량이
  아니라 토큰당 터치 11 GB다. 구문서의 "~9 GB 핫셋"은 이 교정 전의 저계상
  추정치로, 본 문서가 대체한다.)*
- **KV ≈ 0.5 GB/token.**

토큰당 가중치 터치 — 공식: `14·U/A (routed) + 11/A (핫셋) + ~0.5 (KV)`, **h와 무관.**

| 판 | U, A | 상수 | 상태 |
|---|---|---|---|
| K=4 프록시 | A≈3, U(4)≈2.4 | `14·2.4/3 + 11/3 + 0.5` = **15.4GB** | **폐기 (프록시 시절)** |
| **K=1 실측 (현행)** | **A_eff=1.87, U(1)=1.00** | `14·1/1.87 + 11/1.87 + 0.5` = **13.87GB** | **현행 [실측-입력 EST]** |
| K=2 실측 | A_eff=2.47 | = 14.13GB | 현행 밴드 상단 |

**현행 상수 = ~13.9–14.1GB/token [실측-입력 EST]** (K=1 점 13.87; MTP 수락률 실측
r₁=0.87·위치별 급감 → 메모리-바운드 최적 깊이 K=1–2,
[`H_MEASUREMENT.md`](H_MEASUREMENT.md) §133 표).

> **(재도출 2026-07)** 이 절은 §3과 **똑같은 실패**를 하고 있었다: 위 "갱신 2"가 K=4
> 상수 **아래에** 덧붙기만 하고 상수 자체를 재기술하지 않아, 문서가 **15.4를 canonical로
> 선언한 채** 살아있는 모든 헤드라인은 13.87에서 나오는 상태였다. **15.4는 이 문서의
> 헤드라인을 하나도 재현하지 못한다:**
>
> | 헤드라인 | 15.4 기준 | **13.87 기준** | 문서 표기 |
> |---|---|---|---|
> | 1.1TB/s 상주 박스 (§2) | 72 | **79.3** | **80** ✓ |
> | 1.54TB/s v3-proto (§5c) | 100 | **111.0** | **110** ✓ |
> | 1.23TB/s (§5b) | 80 | **88.7** | **89** ✓ |
> | 0.934TB/s 어레이-바운드 (§3) | 61 | **67.3** | **~67** ✓ |
>
> 15.4는 0/4, 13.87은 4/4. **canonical이라 선언해놓고 그로부터 아무것도 도출되지 않는
> 상수는, 이 프로젝트가 없애려는 실패 유형 그 자체다.** 아래 "≈71"도 15.4 시절의 값이라
> 함께 폐기한다.
>
> **주의 — 프리필은 이 상수를 쓰지 않는다.** 프롬프트 토큰은 이미 알려져 있어 투기할
> 것이 없으므로 A=1, U=1이고, 상각 없이 `14 + 11 + 0.5 = 25.5GB/token`을 전부 낸다
> (디코드 대비 **1.84×**). [`USAGE_GAPS.md`](USAGE_GAPS.md) 프리필 행 참조.

**갱신 2(r까지 실측, 잡 B): 상주 박스 ≈ 80 tok/s [실측-입력 EST]**
(GLM-5.2의 더 깊은 MTP가 공표 수준이면 ~95; [`H_MEASUREMENT.md`](H_MEASUREMENT.md)
3차 측정). 적응형 컨트롤러는 이 최적점에 자율 안착 — 설계 검증됨.

## 3. 실리콘: 클록은 목표가 아니라 파생값

불변량: `lane 수 × 클록 ≥ 스트림 소화율`. **어레이는 LPDDR이 실어 나르는 만큼을
소화해야 한다** — 못 하면 병목이 대역폭이 아니라 어레이가 되고, tok/s는 대역폭이
아니라 lane 수가 정한다.

> **(재도출 2026-07)** 이전 판의 표는 소화율 **~650GB/s**에서 파생됐다. 그 값은
> **피벗 전 ONFI 스트리밍 설계점**의 수치이고 [`H_MEASUREMENT.md`](H_MEASUREMENT.md)
> §h-재스코프가 "더 이상 1차 설계점 수치가 아니"라고 명시적으로 폐기했는데, §3만
> 그 결론(lane ~3K @ ~490MHz)을 현행 스펙으로 이월하고 있었다. 상주 박스의 소화율은
> §5 설계점의 대역폭 그 자체(**~1.54TB/s**)다. 아래는 §3 자체 불변식과 자체 마진
> 1.3×를 그대로 쓰고 소화율만 바로잡아 다시 푼 것이다.

**lane 수는 PE_M과 무관하다 [RTL 확인].** `glm_matmul_q4k`에서 가중치는 **열에만
인덱스된다**(`w_q[pj]` = "the weight for row k, col **pj**", PE_N packed — `:20,45`), 그리고
디퀀트된 가중치 `p3_w[pj]`를 **모든 PE_M 행이 공유**한다(`:264` — 각 행은 자기 활성만
곱한다). 즉 **beat당 소비 가중치 = PE_N개**이고 PE_M은 소비율에 들어가지 않는다.
→ **"lane" = PE_N 컬럼 하나**이며, 아래 lane 수는 PE_M 선택과 무관하게 유효하다.
(PE_M은 lane의 *원가*만 바꾼다 — §미해결 참조.)

**바이트/weight는 실측 파일 비율을 쓴다 [실측].** 혼합 타입(Q4_K/Q6_K/Q8_0/F16)이 모두
**같은 beat 구조에서 1 weight/beat**로 처리되므로(`:231-240`의 per-pj `case`), lane 수를
정하는 것은 *초당 weight*이고 바이트/weight는 **실제 체크포인트의 평균**이다:
`UD-Q4_K_XL 467GB ÷ 753B params = 0.62 B/param = 4.96 bit/wt` — §2가 이미 14GB 라우티드
스트림을 계산할 때 쓰는 바로 그 값이다. *(4.5 bit/wt는 순수 Q4_K 144B/256wt **이론값**으로,
실제 혼합 파일을 서술하지 않는다. 이전 판의 이 표는 4.5를 써서 §2와 어긋나 있었다.)*

**결정적 전제 — 풀이 공유냐 전용이냐 [2026-07 추가; 이전 판은 이걸 안 적었다].**
한 토큰(9.0ms)은 세 국면이 **순차로** 흐른다: 어텐션 → MoE → (KV). `glm_decoder_block_q4k`의
FSM이 `T_ATTN` → `T_ESCAN` 순서이고, 어텐션 출력이 있어야 MoE가 시작되므로 **겹칠 수 없다**
(가중치만 미리 당기려면 7.49GB를 온다이 버퍼링해야 하는데 그 SRAM은 §4가 "미설계"로 등록).
그리고 **각 국면 동안 메모리는 1.54TB/s를 그 국면의 바이트로 전부 쏟는다**:

| 국면 | 바이트/토큰 | 소요 |
|---|---|---|
| MoE (routed) | 7.49GB | 4.86ms |
| 어텐션/dense (핫셋) | 5.88GB | 3.82ms |
| KV | 0.50GB | 0.32ms |
| **합** | **13.87GB** | **9.01ms → 111 tok/s** |

→ **어느 국면을 담당하든 그 풀은 평균이 아니라 순간 1.54TB/s를 받아내야 한다**:
`1,540 ÷ (490MHz × 0.62B) = 5,068 lane` (마진 1.3× → **6,588**).

| 구조 | lane | 비고 |
|---|---|---|
| **공유 풀** (한 풀이 세 국면 전부) | **6,588** | 이전 판의 "~6,590"이 바로 이 값 |
| **전용 풀** (RTL 현 구조) | **12,732** | expert **6,144**(3×2,048) + 핫셋 6,588 — 아래 배분 참조 |

**현행 사양 = 전용 12,732 lane.** `mla_attn_q4k`는 matmul을 2개, `swiglu_expert_q4k`는 1개
**자기가 들고** 있어서 서로의 국면에 논다. 그 유휴가 정확히 2× 비용이다. **이전 판의 6,590은
공유 풀 전제 위에 있었고 — RTL은 공유하지 않으므로 2× 과소 사양이었다.** 공유 풀 재설계는
면적 5%p(11.5%→5.7%)를 아끼지만 각 모듈의 matmul을 중앙 풀+아비터로 갈아엎어야 해
**비트 정확 검증 체계를 통째로 재확인**해야 한다 → **기각, 전용 유지.**

**경로별 배분 (이전 판엔 총합만 있고 구조 매핑이 없었다):**

| 풀 | lane | 구성 | 근거 |
|---|---|---|---|
| **expert** | **6,144** | **3 엔진 × TN 2,048** | 아래 |
| **핫셋** | 6,588 | 어텐션 2 + dense/LM-head | MODEL_DIM 6,144 / INTER_DENSE 12,288 / VOCAB 154,880 — 여유 |

> **(정정 2026-07 — 이전 판의 "4 엔진 × 1,647"은 두 가지가 틀렸다. 둘 다 RTL이 반박한다.)**
> - **"차원 상한 2,048"은 거짓.** 실제 상한은 `GW = $clog2((INTER>HIDDEN?INTER:HIDDEN)/TN+1)`
>   (`swiglu_expert_q4k.v:63`)이라 `u_moe`는 **max(2048, 6144) = 6,144**다. TN>INTER는 구조적
>   불가가 아니라 **lane이 노는 것**뿐이다(쓰기 가드 `:188,:215`). 따라서 "2엔진은 3,294가
>   필요해 불가능"이라던 근거는 **성립하지 않는다.**
> - **1,647은 나쁜 숫자다.** INTER_MOE(2048)도 MODEL_DIM(6144)도 나눠떨어지지 않아
>   `GU_FULL`/`DN_FULL`(`:65-66`)이 **둘 다 FALSE**로 접혀 부분-그룹 로직이 살아난다.
>   TN=2048이면 **둘 다 TRUE**(2048%2048=0, 6144%2048=0)로 그 로직이 통째로 사라진다.
> - **엔진 수를 정하는 건 lane이 아니라 평가 횟수다.** 라우팅 top-8 + 공유 1 = **NEVAL+1 = 9회**
>   (`:74`). `ceil(9/3) = ceil(9/4) = 3` → **4번째 엔진은 이득이 0**이다.
> → **3 엔진 × TN 2,048 = 6,144 lane** (목표 6,588의 93%, 마진 1.3× 안). 두 손잡이 모두
>   TN=2048에서 753B 형상 elaborate 확인(`make full-elab-lanes LANE_ELAB_TN=2048`, 101초).
>
> **진짜 병목은 측정됐고, lane도 버스도 T_ACC도 아니었다 — 어텐션이다 [실측].**
> `test/glm_q4k_system_perf_tb.v`에 FSM 상태 히스토그램을 붙여(순수 관측, DUT 미구동) 사이클
> 정확 런으로 분해했다. **모든 lane 손잡이(TN / PE_N / LM_TN)를 4배씩 올린 결과:**
>
> | | cycles/token | 누적 |
> |---|---|---|
> | 기준 (TN=4, PE_N=2, LM_TN=4) | 10,902 | 1.00× |
> | TN 4→16 | 8,244 | 1.32× |
> | PE_N 2→16 | 5,860 | 1.86× |
> | LM_TN 4→16 | **5,731** | **1.90×** |
>
> **lane을 전부 4배 줘서 얻은 게 1.90×**이고, `cycles = 7,358 + 14,176/TN`이 4점 **오차 0.00%**로
> 맞는다(affine in 1/TN). 상태별 분해(lane 최대 시):
>
> | 상태 | 사이클 | 비중 | lane으로 감소 |
> |---|---|---|---|
> | **T_ATTN** | **15,288** | **66.7%** | −38% (PE_N 8× 올려도) |
> | T_EXPW | 2,288 | 10.0% | −74% |
> | idle | 2,170 | 9.5% | −19% |
> | T_FFN_D | 1,408 | 6.1% | −75% |
> | rn1/rn2/route | 2,335 | 10.2% | **0%** |
> | **T_ACC** | **384** | **1.7%** | **0%** |
> | T_ESCAN | 0 | 0% | 죽은 코드 확인 |
>
> - **T_ACC는 1.7%다.** 이전 판이 "9 × 6,144 = 55,296 사이클이 고정이라 speedup이 ~1.7×에
>   묶인다"고 적었던 것은 **분석값이었고 측정이 반증했다. 그 문단을 폐기한다.** 누산 순서(TOPK=8)를
>   비용 중심으로 지목한 것도 함께 폐기 — 고칠 가치가 있는 크기가 아니다.
> - **어텐션이 lane 최대에서도 67%다.** PE_N을 8× 올려도 38%만 준다 — 어텐션 안에 lane과 무관한
>   부분(DSA 인덱서·softmax·KV gather·RoPE)이 크다는 뜻이다.
> - **lane 완전 무관 잔여가 15%** (rmsnorm 7.1% + router 3.1% + T_ACC 1.7% + residual-add 2.2%).
>
> **[비율 충실 재측정 2026-07 — 위 범위 단서가 해소됐다]** 위 표는 `MODEL_DIM=16,
> INTER_MOE=16, TOPK=2`라 실제와 **비율 7개 중 6개가 어긋나 있었다**(최대 5.3×). 그래서 실제
> 비율을 **정확히 재현**하는 축소 config를 만들어 다시 쟀다 — `MODEL_DIM=48, INTER_MOE=16,
> INTER_DENSE=96, TOPK=8, H_HEADS=4, NOPE=24, ROPE=8, V_DIM=32, Q_LORA=16, KV_LORA=4`:
> **MODEL_DIM/INTER_MOE=3.00** (실제 3.00), **H×NOPE/MD=2.00** (2.00), **H×V/MD=2.67** (2.67),
> **Q_LORA/MD=0.33** (0.33) — 전부 일치. 크기만 128× 작다.
>
> | TN | PE_N | LM_TN | cyc/tok | 누적 | **attn%** | expw% | **T_ACC%** |
> |---|---|---|---|---|---|---|---|
> | 4 | 4 | 4 | 53,961 | 1.00× | 50.9 | 27.2 | 1.6 |
> | 16 | 4 | 4 | 36,591 | 1.47× | **75.0** | 10.6 | 2.4 |
> | **16** | **16** | **16** | **23,798** | **2.27×** | **62.6** | 16.3 | **3.6** |
>
> - **T_ACC = 3.6%**. 틀린 비율(1.7%)에서도 맞는 비율(3.6%)에서도 병목이 아니다. 이전 판의
>   "~22% 예상"도 틀렸다. **T_ACC 주장은 두 config에서 독립적으로 죽었다.**
> - **어텐션 62.6%**가 lane 최대에서도 지배한다. TN만 올리면 **75%까지 오른다** — expert 경로를
>   넓혀도 어텐션이 안 줄기 때문이다.
> - **lane 전체 4× → 2.27×**. 루프라인이 약속하는 선형과 멀다.
>
> **어텐션 62.6%를 분해했다 [실측] — 전체의 47%가 lane과 무관한 KV/스코어 경로다.**
> `mla_attn_q4k`의 16개 내부 상태(`:695-710`)를 같은 방식(순수 관측)으로 히스토그램:
>
> | 상태 | 어텐션 내 | 전체 | 하는 일 |
> |---|---|---|---|
> | **S_KEY** | 29.3% | **18.4%** | 유니온 키별 norm/W_uk/W_uv/조립/스코어 |
> | **S_SOFT** | 27.1% | **16.9%** | 헤드별 softmax |
> | **S_CTX** | 18.9% | **11.8%** | 가중 V 컨텍스트 |
> | S_OUT | 10.9% | 6.8% | ctx*W_o (matmul) |
> | S_QUQ | 5.0% | 3.1% | qlora_n*W_uq (matmul) |
> | **S_DSA** | **0.1%** | 0.1% | 인덱서 선택 |
> | S_DSAPF | **0** | 0 | 인덱스 프리페치 |
>
> - **S_KEY+S_SOFT+S_CTX = 어텐션의 75% = 전체의 47%.** 셋 다 **키 수(≈min(S_MAX,TOPK))가
>   정하는 경로**지 matmul lane이 아니다. 어텐션의 matmul 상태 합은 **21%뿐**이고, **PE_N을 8×
>   올려도 어텐션의 79%가 안 줄었던 이유가 이것이다.**
> - **§3의 불변식(`lane × 클록 ≥ 소화율`)이 전체의 47%를 지배하지 못한다.** lane을 아무리 줘도
>   그 47%는 그대로다. **12,732 lane이 111 tok/s를 낸다는 서술은 이 항을 0으로 둔 것이다.**
> - **S_DSA 0.1% / S_DSAPF 0**은 `S_MAX=8 ≤ TOPK_ATTN=8`이라 dense 경로이기 때문 —
>   위 `DSA_REAL_IDX` 미스레딩 항목과 **같은 뿌리**임을 실측이 확인한다.
>
> **세 발견이 하나의 매듭이고, 이제 그 매듭이 실측됐다 [중요].**
> *(이전 판은 이 폭발을 `S_MAX` 외삽으로 적었다 — **틀린 변수였다.** `S_MAX`는 창의 *용량*이지
> *점유*가 아니라서, 8→16으로 올려도 S_KEY/S_SOFT/S_CTX가 **바이트 그대로**였다(실측). 실제
> 변수는 **시퀀스 길이 `s_len`**이다. TB는 `run_token`을 s_len=1,2,3,4로 명시해 부르므로 토큰별
> 비용이 곧 길이별 비용이고, 이미 측정돼 있었다.)*
>
> | s_len | 토큰 비용 | 증분 |
> |---|---|---|
> | 1 | 20,396 | — |
> | 2 | 22,671 | +2,275 |
> | 3 | 24,932 | +2,261 |
> | 4 | 27,193 | +2,261 |
>
> → **`cyc(s_len) = 18,130 + 2,266 × s_len`** — 증분 편차 **0.62%**, 선형 O(n).
>
> **[정정 — 위 선형은 DENSE 구간의 값이다.]** 저 측정은 `TOPK_ATTN=8 = S_MAX=8`, 즉 **고를 것이
> 없는 dense 구간**이었다. "`TOPK_ATTN`이 상한 못 한다"고 적었던 것은 **틀렸다.** `TOPK_ATTN=2`로
> **sparse 구간을 강제해 다시 재니 비용이 포화한다**:
>
> | s_len | DSA_REAL_IDX=0 | DSA_REAL_IDX=1 |
> |---|---|---|
> | 1 | 18,380 | 18,380 |
> | 2 | 20,655 (+2,275) | 20,655 (+2,275) |
> | 3 | 21,608 (+953) | 21,644 (+989) |
> | 4 | 21,617 (**+9**) | 21,665 (**+21**) |
>
> **s_len > TOPK_ATTN이 되는 순간 증분이 붕괴한다.** 즉 **어텐션은 O(n)이 아니라
> `O(min(s_len, TOPK_ATTN))`**이고, 실사용 컨텍스트에서는 **O(1)**이다. 컨텍스트를 올려도
> 어텐션은 폭발하지 않는다 — **이전 판의 5,461× 외삽을 폐기한다.**
>
> **그리고 `DSA_REAL_IDX`가 정하는 것은 비용이 아니라 정답이다 [실측].** 켜면 `S_DSAPF`가
> 0 → **84**로 실제 프리페치가 돌고, 비용은 21,617 → 21,665로 **+0.2%**뿐이다.
> **쿼리 의존 선택은 사실상 공짜다** — 지금 그것을 켜지 않아 얻는 이득이 없다는 뜻이고, 끄고
> 있는 대가는 (위 `DSA_REAL_IDX` 항목대로) 모든 쿼리가 앞 k개 키만 보는 것이다.
>
> **시스템 레벨에서 `=1`이 정합함을 확인했다 [실측, 최초].** 지금까지 `=1`은 **리프에서만**
> 증명돼 있었다(`make mla-sparse`: PE_M=3에서 per-row q-dependent DSA가 비트 정확, dense+sparse
> 양쪽). `glm_q4k_system` 전체(expert cache + KV pager + xbar + loader 포함)로 `=1`을 돌린 적은
> **없었다** — perf TB가 그 파라미터를 넘기지 않아 **돌릴 방법 자체가 없었기 때문**이다.
> 연결 후 sparse config(TOPK_ATTN=2 < S_MAX=8)에서:
>
> | | 토큰 (s_len 1~4) | 시스템 vs standalone ref |
> |---|---|---|
> | DSA=0 | 12, 14, 14, 14 | **불일치 0** |
> | **DSA=1** | 12, 14, **2, 2** | **불일치 0** |
>
> **둘 다 ref와 완벽히 일치하면서 토큰 값이 갈린다** — `TOPK_ATTN=2`이므로 **s_len>2부터 선택이
> 시작되는 지점과 정확히 일치**하고, `S_DSAPF` 0→84와도 맞는다. **즉 `=1`은 정합성을 유지한 채
> 실제로 다른(쿼리 의존) 키를 고른다.**
>
> **컨텍스트 창을 키워도 비용이 평평하고 정합성이 유지된다 [실측, 최종].**
> `DSA_REAL_IDX=1`, `TOPK_ATTN=2` 고정, `S_MAX`만 8→64:
>
> | S_MAX | 시스템 vs ref | cyc/tok | vs 8 | S_KEY / S_SOFT / S_CTX |
> |---|---|---|---|---|
> | 8 | 일치 | 20,586 | 1.000× | 12,236 / 8,064 / 9,728 |
> | 16 | 일치 | 20,602 | 1.001× | **동일** |
> | 32 | 일치 | 20,634 | 1.002× | **동일** |
> | **64** | **일치** | **20,698** | **1.005×** | **동일** |
>
> **창을 8× 키우는 비용이 +0.5%이고, KV/스코어 사이클은 바이트 그대로다** — `TOPK_ATTN`이
> 키 수를 완벽히 상한하므로 창 크기가 어텐션 비용에 들어가지 않는다. 정합성은 네 점 모두 유지.
> **즉 어텐션은 `O(min(s_len, TOPK_ATTN))`이라는 앞의 결론이 창 축에서도 확인됐고, 컨텍스트
> 확대의 진짜 비용은 연산이 아니라 KV 용량(87.8 KB/token, §5c)뿐이다.**
>
> **→ `=1`을 막을 이유가 프로덕션 구성에는 없다.** RTL이 적어둔 유일한 제약은
> **`PER_ROW_SEQ=1`이면 `DSA_REAL_IDX=0`이 필요하다**는 것인데(`mla_attn_q4k.v:152-153`,
> "the shared-prefix kidx_buf prefetch is not yet per-seq"), **`glm_q4k_system`에는 `PER_ROW_SEQ`가
> 아예 없다**(기본값 0 — 단일 컨텍스트 결정, `USAGE_GAPS` 참조). **멀티컨텍스트를 비-목표로 둔
> 결정이 여기서 배당을 준다.**
>
> **→ 매듭은 실재하지만 내가 묶은 방향이 틀렸다.** 어텐션 비용은 `TOPK_ATTN`이 상한하므로
> **컨텍스트 확대가 성능을 폭발시키지 않는다.** 남는 것은 **정확도** 쪽이다: `DSA_REAL_IDX=0`인
> 채로 창을 올리면 **비용은 O(1)로 잘 상한되면서 모든 쿼리가 앞 k개만 보는** 상태가 된다 —
> 즉 **빠르고 조용히 틀린 물건**이 나온다. 그리고 그것을 고치는 비용이 **+0.2%로 측정됐다.**
> **컨텍스트 창을 올리기 전에 `DSA_REAL_IDX=1`을 켜야 하고, 그 대가는 사실상 없다.**
>
> **sparse 구간(실사용 조건)에서도 쟀다 — §3의 lane 서술은 성립하고, 정도만 완화된다 [실측].**
> `TOPK_ATTN=2 < S_MAX=8`, `DSA_REAL_IDX=1`:
>
> | | dense (TOPK_ATTN=8) | **sparse (실사용)** |
> |---|---|---|
> | cyc/tok (lane 최대) | 23,798 | **20,586** |
> | **어텐션** | 62.6% | **56.7%** |
> | **KV/스코어 (lane 무관)** | 47.1% | **36.5%** |
> | lane 4× 이득 | 2.27× | **2.40×** |
>
> **실사용 조건에서도 어텐션이 56.7%로 지배하고, 전체의 36.5%는 lane이 줄이지 못한다.**
> lane을 4× 줘서 얻는 것은 **2.40×**다. §3이 `lane × 클록 ≥ 소화율` 불변식으로 12,732 lane을
> 세우고 111 tok/s를 약속하는데, **실측 RTL은 실사용 조건에서 lane 4×에 2.40×라고 말한다.**
> 완화되긴 했다(47.1% → 36.5%) — 하지만 사라지지 않는다.
> *(절대 크기는 여전히 축소 config 기준 — 배수와 선형성이 옮겨지는 것이고 절대 사이클은 아니다.)*

> **[남은 [측정필요]]** 절대 크기(6144 vs 48)는 여전히 다르다. 비율이 같으니 *비중*은 옮겨지지만,
> 절대 사이클과 tok/s는 아니다. 실형상 런은 **verilator로도 비현실적**임이 확인됐다: 생성 C++이
> 6,977 파일/4.4GB이고 `-Os` 단일 스레드로 22시간 (`-j16 -O0`로도 ~30분 + 메모리). 그리고 그
> 런은 **weight loader가 PE_N을 16에서 막는 버그**(아래 — pj_iss 폭, 2026-07 근본원인 규명+수정) 때문에
> 어차피 가중치가 0인 채 도는 셈이었다. 그 "가중치 0" 증상이 바로 이 버그의 서명이다(초과 컬럼 헤더 미로드).
>
> *(부수 정정: `T_ESCAN`은 커밋된 PE_M=1에서 **진입하지 않는 죽은 코드**다(`:812` "expert union
> scan (PE_M>1)"). 직렬화는 `T_ACC` 안의 `exp_i`/`sel_e[0][exp_nxt]` 루프(`:857-872`)에 있다.
> 이전 판이 T_ESCAN을 병목으로 지목한 것은 잘못된 FSM을 가리킨 것이다.)*

(1.1TB/s인 16팩 변형(→~80 tok/s)은 같은 식으로 전용 **~9,412 lane**; LPDDR6 1.65TB/s면
**~14,118**.)

- **RTL에 커밋된 것은 `PE_N = 4`다** — 슬라이스도, 진짜 GLM-5.2 풀컨피그도
  (`configs/full_glm52.vh`: `GLM52_PE_N 4`). **`3,072`는 RTL·configs 어디에도 없다**;
  이전 판 표의 숫자였을 뿐이다. 즉 **아무도 실리콘급 PE_N으로 elaborate해본 적이 없고**,
  위 12,732는 **전부 미구현 목표치**다.
- **110 tok/s는 어레이가 12,732 lane이라는 전제 위에서만 성립한다.** 이 전제를 명시하지
  않은 채 110을 말하는 것은 과대주장이다. (참고로 이전 판이 말하던 3,072라면 소화율
  `3072 × 490MHz × 0.62B = 0.934TB/s` → `÷ 13.87GB` ≈ **~67 tok/s**에 묶인다.)
- **노드 선택의 바닥은 여전히 LPDDR5X PHY IP** (벤더 제공 7–16nm) → **12–16nm +
  코어 ~500MHz**. 바뀐 것은 클록이 아니라 **lane 수**다.
- FPGA 캠페인의 비트 정확 스테이지 분해(rope 10단/act 20단/matmul 5단)가 그대로 이월.

**미해결 (재도출이 만든 숙제 — 정직하게 미검증으로 남긴다):**
- **투기 디코딩과 메모리 시스템이 합쳐진 탑이 없다 [RTL, 2026-07 발견 — 헤드라인을 직접 친다]**:
  `glm_q4k_system_cdc`(프로덕션 2-clock 탑)는 **`glm_q4k_system` 하나만** 감싼다. 그런데
  `glm_q4k_system`이 `glm_model_q4k`에 넘기는 25개 파라미터에 **`PE_M`이 없다**(`SWIN`·
  `PER_ROW_POS`·`PER_ROW_SLEN`도 없음) → 모델 기본값 **PE_M=1**. 그리고 `PE_M = DRAFT_K+1`
  (`spec_batched_top.v:13`)이므로 **PE_M=1 = K=0 = 투기 없음**이다.
  반대로 스펙체인 탑들(`spec_decode_top` 등)에는 **메모리 시스템이 0개**다 —
  `expert_cache_pf`/`kv_cache_pager`/`ddr5_xbar`/`weight_loader_q4k` 어느 것도 인스턴스하지
  않고 `glm_model_q4k`를 직접 든다. **즉 투기 OR 메모리 시스템이지 둘 다가 아니다.**

  **이것이 §2/§5의 헤드라인을 직접 친다:**

  | 구성 | 상수 | 1.54TB/s에서 |
  |---|---|---|
  | K=1 투기 (§2 현행, A_eff=1.87 실측) | 13.87 GB/tok | **111 tok/s** |
  | **K=0 = 투기 없음 (프로덕션 탑의 실제 구성)** | **25.50 GB/tok** | **60 tok/s** |

  **더 정확히: A로 나누는 것 자체가 `spec_batched_top`에만 있다 [RTL].** 스펙 탑은 셋인데
  `glm_model_q4k`에 **`PE_M`을 넘기는 것은 `spec_batched_top`(`.PE_M(B)`) 하나뿐**이다.
  `spec_decode_top`은 안 넘겨 PE_M=1이고, `spec_batched_top.v:8-9`가 그 뜻을 직접 적는다 —
  *"The committed K=1 loop (spec_decode_top) runs **ONE full glm_model_q4k weight-load to
  verify ONE drafted position** per pass"*. **상각이 0이라는 뜻이다.** §2의 `14·U/A + 11/A`가
  A로 나누는 근거는 **한 번의 가중치 로드로 K+1행을 검증**하는 것이고, 그것은 `PE_M=B`,
  즉 `spec_batched_top`뿐이다.

  **그런데 그 탑도 `DSA_REAL_IDX`를 넘기지 않는다(=0).** 즉 배치 검증을 하는 유일한 탑에서도
  어텐션은 쿼리와 무관하게 앞 k개만 본다. 그리고 그 탑은 (헤더 정정에서 확인했듯) **위치 정확
  인과 검증이 아니다** — 모든 행이 `.pos(cur_pos)` 공유 스칼라다.

  **정리하면 A_eff=1.87을 실제로 내려면 한 탑이 동시에: (a) 메모리 시스템, (b) `PE_M=K+1`,
  (c) 위치 정확 검증(`PER_ROW_POS`), (d) `DSA_REAL_IDX=1`을 가져야 하는데, 넷을 다 가진 탑은
  없고 어떤 탑도 셋 이상을 갖지 못한다.**

  **§2의 정본 상수와 §5의 110 tok/s는 프로덕션 탑이 낼 수 없는 구성의 값이다.** 닫으려면
  `glm_q4k_system`이 `PE_M`(+`SWIN`, `PER_ROW_POS/SLEN`)을 스레드하고 스펙체인을 품어야 하며,
  그건 **[설계필요]**다.

  > **⚠️ SUPERSEDED (아래 "(완료 2026-07, Step 5 = spec composition)" 노트가 이 진행 노트를 대체함 —
  > 이 노트의 "111은 여전히 미실현"은 Step 5 완료로 폐기됐다; 감사 이력으로만 보존).**
  >
  > **(진행 2026-07, KV write-back)** 위 (c) *위치 정확 검증*의 근본 전제 — `spec_batched_top`이
  > *"the computed latent is write-only, so draft j cannot attend to draft j-1"*이라 밝힌 **다이 내부
  > KV write-back 경로** — 이 이제 **실제로 구축·검증됐다**(`docs/KV_WRITEBACK_DESIGN.md`, Steps 1–3):
  > MLA가 계산한 latent(`kv_lat_row`)을 노출→`glm_q4k_system`이 `SELF_KV=1`에서 pager append로 쓰고
  > pager gather로 되읽어, **다이가 자기가 만든 per-(layer,position) KV를 attend**한다. L=1·L=6 모두
  > 독립 참조 대비 **full-logit 비트정확**(`make self-kv-roundtrip`/`self-kv-l6-roundtrip`), `SELF_KV=0`은
  > byte-identical(`make self-kv-equiv`), 레이어 앨리어싱·append 손상 주입 모두 잡힘. 즉 스펙 검증을
  > *위치 정확*하게 만들 재료가 이제 있다. **남은 것은 (a)(b)(d)와 이 KV 경로를 한 탑에 합쳐
  > draft-verify 루프를 품는 것**(Step 5) — 그때 A_eff=1.87이 실측 가능해진다. ~~아직 프로덕션 탑의
  > 실측은 60 tok/s(K=0)이며, 111은 여전히 **미실현 설계점**이다~~ **[이 문장은 폐기 — 아래 완료 노트 참조:
  > Step 5가 `glm_q4k_spec_system`으로 완료됐고 A_eff는 하드웨어 `weight_loads` 카운터 실측이 됐다.]** *(관련: `SWIN` 기본식 `min(S_MAX,TOPK_ATTN)`의 안전성 논증은
  `mla_attn_q4k.v:113-118`이 밝히듯 **한 행이 최대 TOPK개를 고른다**는 것 — 즉 **PE_M=1 전용**이다.
  PE_M>1이면 행별 선택의 union이 최대 `PE_M*TOPK`라 기본식이 PE_M× 부족하고, 이를 증명 TB는
  알고 있어 `SWIN=PE_M*TOPK`를 명시한다(`test/mla_attn_q4k_sparse_perrow_tb.v:99`). 그런데
  **`glm_q4k_system`엔 `SWIN` 파라미터가 없어 키울 수도 없고, assertion도 없다.**)*

  > **(완료 2026-07, Step 5 = spec composition)** 위 진행 노트가 "남은 것"으로 지목한 **한 탑에
  > (a)(b)(c)(d)+KV write-back을 다 합쳐 draft-verify 루프를 품는 것**이 이제 **구축·검증됐다**:
  > `src/glm_q4k_spec_system.v`가 `glm_q4k_system`을 `PE_M=K+1`·`SELF_KV=1`·`INTRA_CAUSAL=1`·
  > `PER_ROW_POS=1`·`KV_EXT_APPEND=1`로 인스턴스화하고 `spec_decode_seq`를 얹어 넷을 동시에 만족한다.
  > 위치 정확 배치 검증의 진짜 블로커였던 **intra-batch 인과 어텐션**(한 배치 안에서 행 j가 같은 패스에서
  > 계산된 행 0..j-1의 현재키를 인과적으로 attend)을 `mla_attn_q4k`에 리프 기능으로 넣어 **배치 검증 ==
  > 직렬 디코드 full-logit 비트정확**으로 증명했고(`make intra-batch-verify`), 조합 탑은 **spec==greedy
  > 불변식** — 커밋 스트림이 `PE_M=1` greedy 디코드의 비트정확 프리픽스 — 을 K=1,2,3 × {ACCEPT,REJECT,
  > MIXED}에서 만족한다(`make spec-greedy`, `ALL 31 TESTS PASSED`; raw-draft 커밋 주입·거부행 phantom
  > KV 주입 모두 FAIL로 잡힘).
  >
  > **A_eff는 이제 [실측-입력 EST]가 아니라 하드웨어 카운터 실측이다.** `glm_q4k_spec_system`의
  > `weight_loads` 레지스터가 패스당 정확히 1회(K+1행을 한 로드로 검증) 증가함을 게이트가 tb 패스수와
  > 교차검증하고(faithfulness), `A_eff = total_tokens/weight_loads`를 출력한다. ALL-ACCEPT가 정확히
  > **K+1 토큰/1 weight-load** 천장을 친다(K=3 → **4.00 tok/load → 6.37 GB/token**); 바닥은 무투기 그대로
  > **1.00 → 25.50**. 즉 **"한 가중치 로드로 K+1행 검증"이라는 §2가 A로 나누는 바로 그 상각이 이제
  > 실현·측정된 RTL이다** — 더는 미실현 설계점이 아니다.
  >
  > **RTL이 정하지 못하는 유일한 입력은 프로덕션 수락률 `r`**(모델 MTP draft 품질) — 박스가 어느 열에서
  > 도는지를 고른다. 실측 `r₁=0.87`(`H_MEASUREMENT.md`) → `A_eff=1.87` → 13.87 GB/token → **1.54 TB/s에서
  > 111 tok/s**(1.1 TB/s 상주 박스면 ~80). **오버클레임 아님**: 상각 메커니즘은 실측 RTL, 그를 먹이는
  > 수락률은 실측 모델 속성, tok/s는 둘의 곱이다. `111 = 미실현 설계점`은 이 노트로 폐기하고
  > `111 = (실측 A_eff 메커니즘) × (실측 수락률)`로 대체한다.
- **가중치 경로가 lane을 PE_N≤16에서 막았다 → ~~[설계필요]~~ RTL은 해소, 시스템/물리만 남음 (2026-07)**:
  두 겹이었다. **(1) DATA_W cap**: `weight_loader_q4k.v:231,237`이 `rd_data[16*PE_N-1:0]`을
  `DATA_W=256` 버스에서 part-select → `16·PE_N ≤ DATA_W`. iverilog가 범위 초과를 경고 없이 0으로
  채우던 것을, `16*PE_N > DATA_W`이면 **양쪽 툴에서 loud $error**로 강제하는 elaboration 가드로 명시화
  (7e43838). **(2) 진짜 blocker — pj_iss 폭 버그**: 컬럼 인덱스 `pj_iss`(0..PE_N-1)가 컬럼이 아니라
  **슈퍼블록 폭 `SBW=$clog2(NSB+1)`**로 선언돼 `PE_N > 2^SBW`에서 wrap → 컬럼이 서로 헤더 슬롯을
  덮어써 초과 컬럼 출력이 0. 출하 기본(PE_N=4, WL_KMAX=256→NSB=1→SBW=1)에서 이미 컬럼 2,3을
  손상시켰으나, 로더 테스트는 NSB=3으로, 모델은 loader를 아예 안 써서(가중치를 직접 포트로 받음)
  가려져 있었다. `pj_iss`를 `PJW=$clog2(PE_N+1)`로 수정(6a32b5a). **검증**: loader→GEMM ggml 골든
  비트정확 PE_N=4/8/16/32/64 전부 통과(이전엔 8부터 실패), `make weight-loader-lanes`(PE_N=32)로
  release-gate에 상주(24fd16c). `weight-ecc-equiv`는 넷리스트가 바뀌어 ECC-물리제거 참조본
  (`test/ref/weight_loader_q4k_noecc_ref.v`)으로 재베이스라인, 여전히 PROVEN.
  **남은 [설계필요]**: 로더 RTL은 이제 임의 PE_N을 지원하지만, 제품이 요구하는 큰 lane(예 512/2048)을
  실제로 내려면 시스템이 `WL_DATA_W ≥ 16·WL_PE_N`(= 8,192b/32,768b)를 넘겨야 하고 그건 **물리 메모리
  버스 폭 결정**이다 — RTL 장벽이 아니라 시스템/물리 통합.
- **면적 — 게이트 수는 실측됨 (2026-07, `tools/lane_area_sweep.sh` — 재현 가능)**:
  `glm_matmul_q4k`의 `PE_N` 컬럼 하나 = Q4_K dequant 1개 + PE_M개 MAC = **§3 자체의
  "lane" 정의 그대로**다. 그래서 PE_N을 스윕한 **기울기**가 lane당 비용이고, 이 방식이
  고정 제어/FSM 오버헤드를 소거한다(절대값 하나만 재면 그게 섞여 들어간다).
  yosys 0.66, `abc -g simple`, PE_M=4, KMAX=32:

  | PE_N | NAND2 등가 게이트 |
  |---|---|
  | 1 | 41,563 |
  | 2 | 80,787 |
  | 3 | 121,550 |

  PE_M=4에서 → lane당 **39,994 GE**, 절편 1,570, 1→2가 39,225·2→3이 40,763으로 **선형성
  편차 3.8%**. (KMAX 무관 확인: PE_N=1이 KMAX=256에서 44,747, KMAX=32에서 41,563 → 차
  3,184는 **카운터 폭 = 고정 오버헤드**라 기울기가 상쇄한다.)

  **lane 원가는 PE_M에 선형이다 [실측]** — weight당 `(3+PE_M)`곱 + `(1+PE_M)`덧셈이므로:

  | PE_M | weight당 연산 | lane당 GE (실측) | 비고 |
  |---|---|---|---|
  | 1 | 4곱+2덧셈 | **20,668** | 스펙체인 없음 (K=0) |
  | **2** | 5곱+3덧셈 | **26,621** | **← 실측 최적 K=1** |
  | 3 | 6곱+4덧셈 | ~33,600 [적합] | K=2 (실측 동률) |
  | 4 | 7곱+5덧셈 | **39,994** | K=3 — **실측이 기각한 구성** |

  (선형 적합 `lane(PE_M) = 14,225 + 6,442·PE_M`은 PE_M=2를 1.8% 오차로 맞힌다.)

  **판정 — §3의 두 진술 중 "다이 수 mm²"만 틀렸다.**
  - **게이트 수 "2–3만/lane"은 맞다**: 실측 최적 PE_M=2에서 **26,621 GE = 그 범위 안**.
    *(이전 판이 "1.3–1.6× 과소"라 적었던 것은 **실측이 기각한 PE_M=4에서 쟀기 때문**이며,
    그 판정을 철회한다. §3의 추정은 자기가 고른 구성에서 옳았다.)*
  - **"다이 수 mm²"는 안 나온다**: 실측 PE_M=2로도 전용 12,732 lane이 338.9M GE →
    **38.7–53.3mm²**다. 이전 판이 말하던 3,072 lane 시절에도 9.3–12.8mm²로 이미
    **수 mm²가 아니었다.**

  **현행 실측 기준: 전용 12,732 lane × 26,621 GE = 338.9M GE → 38.7–53.3mm² =
  480mm² 다이의 8.1–11.1%** [EST]. *(공유 풀이었다면 6,588 lane = 175M GE = 4.2–5.7%.
  전용 유지 결정의 대가가 이 5%p다 — 위 §3 본문 참조.)* 480mm² 패드-리미티드 다이(1280-bit PHY beachfront가 크기를 결정)에 코어
  여유가 있을 개연성은 있으나, **"면적은 공짜"라고 말하지 않는다** — 그리고 반대로
  **"안 들어간다"고도 말하지 않는다.** 방어 가능한 진술은 *"공짜는 아니지만 ~8–12%로
  감당 가능하며, 병목은 여전히 실리콘 NRE"*까지다.
  **지배적 불확실성은 이제 게이트 수가 아니라(그건 실측됐다) 0.08–0.11 µm²/GE라는 외부
  밀도 가정이다**
  [EST, external — 저장고 출처 아님]. 이 기계엔 OpenLane/sky130/magic이 없고 sky130은
  130nm라 12–16nm 부품엔 애초에 틀린 노드다 → **진짜 PDK 수치는 [측정필요]로 남는다.**
  또 `glm_matmul_q4k`는 스스로 "the CORRECT-FIRST reference core"(`:32`)라 밝히며 beat당
  **조합형** fp32 dequant+MAC이다 — ASIC lane은 재파이프라인되면 작아질 수도(공유)
  커질 수도(리타이밍 레지스터) 있다. **이 수치를 "실측 면적"으로 인용하지 말 것.**
- ~~**lane 정의 [미해결]**~~ — **해소됨 (2026-07, 실측 기준). PE_M은 자유 선택이 아니었다.**
  세 가지가 이 충돌을 없앤다:
  1. **lane 수는 PE_M과 무관**하다(위 §3 본문, RTL 확인). 그러니 "lane 정의"가 lane
     *개수*를 흔든다는 전제 자체가 틀렸다 — PE_M은 lane의 **원가만** 바꾼다.
  2. **PE_M은 배치가 아니라 스펙체인 검증 폭이다**: `spec_batched_top.v:13`이
     **`PE_M = DRAFT_K+1`**로 정의한다. 그래서 "단일 사용자 박스에 batch-4는 노는
     실리콘"이라는 §1의 반론은 **PE_M에 적용되지 않는다**(그 반론은 멀티유저 배치에 대한
     것이고, PE_M은 한 시퀀스의 draft 검증 행이다). §1과 §3은 애초에 충돌하지 않았다.
  3. **K는 이미 실측으로 정해져 있다**: [`H_MEASUREMENT.md`](H_MEASUREMENT.md) §131 표의
     실측 A_eff/α로 **K=1이 최적(~81 tok/s), K=2가 동률(~80)**, K=3은 ~73, K=4는 ~66이다.
     `PE_M = K+1` → **PE_M = 2–3이 실측이 고른 값이고, §3이 쓰던 PE_M=4(=K=3)는 자기
     프로젝트의 실측이 최적보다 10% 나쁘다고 말한 지점이다.**

  → **현행: lane = `dequant + 2–3 MAC` (PE_M=2–3, 실측 최적 K=1–2).** §3의 "dequant+4MAC"
  표현은 실측 이전 값이라 폐기한다. 아래 면적/§4 전력의 PE_M=4 수치는 **상한**으로만 읽어야
  한다(K=3 구성의 값이며, 실측이 고른 구성이 아니다).
- ~~**bit/weight 불일치**~~ — **해소됨 (2026-07, 실측 기준).** §3이 쓰던 `4.5 bit/wt`는
  순수 Q4_K **이론값**이고 실제 파일을 서술하지 않았다. 닫는 데 필요했던 질문("혼합 양자화
  블록에서도 lane이 사이클당 1 weight를 소화하는가?")은 **RTL이 답한다: 그렇다** —
  `glm_matmul_q4k.v:231-240`의 per-pj `case`가 Q4_K/Q6_K/Q8_0/F16을 **모두 같은 beat
  구조**에서 처리한다. 따라서 lane 수를 정하는 것은 초당 weight이고 바이트/weight는 실제
  체크포인트 평균 **0.62 B/param (4.96 bit)** — §2가 이미 쓰던 값이다. 위 표를 그 기준으로
  다시 풀었다: **공유 풀 기준 6,588 lane**(7,263 아님; 전용인 현 구조는 12,732 — 위 참조), 3,072의 소화율 **0.934TB/s**(0.847 아님),
  **1.65× 부족**(1.82× 아님), 어레이 바운드 **~67 tok/s**(~60 아님).
- **전력 [미검증]**: ~~§4의 ~40–60W는 1.1TB/s에서 도출됐다~~ — **이 진단 자체가 틀렸다
  (2026-07 정정).** §4의 ~25–40W 레일은 **1.1TB/s에서 도출된 게 아니라, 어느 설계점에서도
  도출된 적이 없다**: 역산하면 ~780–830GB/s를 함의하는데 그런 설계점은 이 프로젝트에
  존재한 적이 없다(§4 재도출 블록). 피벗 누락이 아니라 **피벗 이전부터의 산수 오류**이며,
  §5c가 그걸 ×1.4 해서 실어 날랐다. 결론(§4는 v3-proto의 검증된 예산이 아니다)은 그대로
  유효하고, 오히려 더 강해진다 — §4는 **어느 설계점의 예산도 아니었다.** 1.54TB/s에서
  §4 자체 계수로 LPDDR만 **49–74W**, 어레이도 1.8× 빠르게 일하므로 SoC가 함께 오른다.
  **SoC 항목은 [미검증]이므로 총합은 `≥64–99W`라는 하한이지 예산이 아니다.**

## 4. 전력 (v1→v3 개정 이력 — 정직하게)

| 판 | 총 전력 | 오류/변경 |
|---|---|---|
| v1 | ~35–55W | **NAND 읽기 에너지 과소평가** |
| v2 | ~70–120W | 물리 교정: NAND 읽기 ~25–55pJ/bit → 200GB/s 지속 = **40–90W가 열 예산 지배** (SSD 실측 ~100–135pJ/bit에서 컨트롤러/PCIe 제거분 차감) |
| **v3** | **≥50–78W [EST, 재도출 2026-07]** | 상주 피벗으로 NAND 스트림 항목 자체가 소멸(이 결론은 유효). LPDDR(~1.1TB/s, 4–6pJ/bit)가 새 지배 레일 **35–53W** + SoC **미검증**. **구 ~25–40W / 총 ~40–60W는 폐기** — 아래 참조 |

> **(재도출 2026-07 — 이건 피벗 누락이 아니라 산수 오류다)** 위 v2 행이 이 표의 방법을
> 스스로 증명한다: `200GB/s × 8 × 25–55pJ/bit = 40–88W` → 표기 "40–90W" ✓. **방법 =
> 바이트/s × 8 × pJ/bit.** 같은 방법을 v3 행 자신의 4–6pJ/bit에 적용하면:
>
> | 설계점 | LPDDR 레일 (자체 계수) |
> |---|---|
> | 650GB/s (스트리밍, 폐기) | 20.8–31.2W |
> | **1.1TB/s (v3 행 자체 전제)** | **35.2–52.8W** |
> | **1.54TB/s (v3-proto, §5c)** | **49.3–73.9W** |
>
> **구 ~25–40W는 셋 중 어디에도 없다.** 역산하면 `25W ÷ (8×4pJ) = 781GB/s`,
> `40W ÷ (8×6pJ) = 833GB/s` — **~780–830GB/s 설계점은 이 프로젝트가 가져본 적이 없다.**
> 즉 이 값은 *낡은* 게 아니라 **애초에 도출된 적이 없다**. 피벗 이전부터 틀렸고, §5c가
> 그것을 ×1.4 해서 앞으로 실어 날랐다(§5c 참조).
>
> **SoC 15–25W는 폐기한다 — 틀려서가 아니라 도출된 적이 없어서다.** 저장고 어디에도
> lane의 pJ/op도 게이트밀도→W 모델도 없었고, 이 값은 §3이 3,072 lane이라 하던 시절에
> 쓰였다(§3은 이제 전용 **12,732** — 실측 0.62 B/param + 전용 풀 기준). **2026-07에 산술 항만은 RTL에서 처음으로 도출됐다. 나머지는
> 이름을 붙여 [미검증]으로 남긴다.**
>
> **weight 하나가 시키는 일 [REPO] — "dequant 1 + MAC 1"이 아니다.** `glm_matmul_q4k.v`의
> 컬럼당·beat당 실제 연산: dequant = **fp32 곱 ×3 + 덧셈 ×1** (`:216` d×sc_b, `:218`
> dmin×m_b, `:238` ×q코드, `:254` −dmin·m_b — 헤더 `:22`의 공식
> `w=(d*sc_b)*q−(dmin*m_b)` 그대로), MAC = **곱 ×PE_M**(`:264`) + **덧셈 ×PE_M**(`:270`).
> → weight당 `(3+PE_M)` 곱 + `(1+PE_M)` 덧셈. **dequant는 1연산이 아니라 4연산이다.**
>
> **산수 [EST, external].** Horowitz ISSCC 2014 45nm: fp32 곱 3.7pJ / 덧셈 0.9pJ.
> 45nm→12–16nm 에너지 감소 **2.5–4×** [EST].
>
> **[정정 2026-07] 아래 표는 원래 2.74T wt/s(4.5 bit/wt)에서 계산됐다. 실측 기준
> 2.48T wt/s(0.62 B/param)로 다시 풀고, PE_M도 실측이 고른 값으로 바꾼다** — `PE_M=K+1`
> 이고 실측 최적은 **K=1–2 → PE_M=2–3**이다(§3 lane 정의 항목).
>
> | 구성 | weight당 연산 | pJ/weight @45nm | @12–16nm | 산술 항만 @**2.48T wt/s** |
> |---|---|---|---|---|
> | PE_M=1 | 4곱+2덧셈 | 16.6 | 4.2–6.6 | 10–16W |
> | **PE_M=2** (실측 최적 K=1) | 5곱+3덧셈 | **21.2** | **5.3–8.5** | **13–21W** |
> | PE_M=3 (K=2 동률) | 6곱+4덧셈 | 25.8 | 6.5–10.3 | 16–26W |
> | PE_M=4 (K=3) | 7곱+5덧셈 | 30.4 | 7.6–12.2 | 19–30W | **← 실측이 기각** |
>
> **실측이 고른 구성(PE_M=2–3)에서 산술 항만 13–26W다.** 옛 15–25W와 겹치지만 **그건
> 우연이다 — 옛 값은 도출된 적이 없고, 이 값은 SRAM·인터커넥트·클록트리·누설·PHY를 아직
> 하나도 안 담았다.** 그러니 15–25W는 여전히 폐기한다: **"틀렸다"가 아니라 "근거가 없었고,
> 근거 있는 하한이 이제 그 범위 안에서 시작한다"**는 뜻이다. 총합은 그대로 `≥`다.
>
> **선행질문 — 해소됨 (2026-07, 실측 기준).** "lane이 PE_M=4냐 PE_M=1이냐"는 **설계 결정이
> 필요한 미해결이 아니었다.** `PE_M = DRAFT_K+1`(`spec_batched_top.v:13`)이고 K의 최적은
> **실측되어 있다**(K=1이 ~81 tok/s로 최적, K=2 ~80, K=3 ~73, K=4 ~66 —
> [`H_MEASUREMENT.md`](H_MEASUREMENT.md) §131) → **PE_M=2–3**. §3의 lane 정의 항목 참조.
> 또 PE_M은 멀티유저 배치가 아니라 한 시퀀스의 draft 검증 행이므로 §1의 "batch-4는 노는
> 실리콘" 논리는 애초에 PE_M에 적용되지 않는다 — §1과 §3은 충돌한 적이 없다.
>
> **여전히 [미검증] — 이름을 정확히 붙인다:**
> - **온다이 스테이징 SRAM — 미측정이 아니라 미설계.** `P2_MEMORY_MAP.md`는 신뢰성 *분류*
>   문서로 배열이 기호폭뿐 숫자 크기가 없고, 모델 페이로드는 **전부 오프다이**라 명시한다
>   (`:32`). 12,732 lane은 오퍼랜드를 어딘가에 스테이징해야 하는데 **그 메모리가 RTL에 없다.**
> - **인터커넥트 — 약한 하한만.** TPUv1 실측 전칩 0.87pJ/MAC이 함의하는 비산술 오버헤드
>   ~0.46pJ/MAC은 weight 재사용 ~256×인 시스톨릭에서 나온 값이다. **재사용이 없는 스트리밍
>   구조는 그보다 크지 작지 않다.**
> - **SoC측 LPDDR5X PHY — 총합 최대 미지수.** 아래 별도 블록.
> - **클록트리**: 플로어플랜이 없어 못 묶는다.
> - **위 산술 값 자체가 상한이다**: 이 코어는 스스로 "the CORRECT-FIRST reference core"라
>   밝힌다(`glm_matmul_q4k.v:32`) — ASIC lane은 재파이프라인되면 달라진다.
>
> **결론: SoC에 점값을 붙이지 않는다. 총합은 계속 `≥`다. 1.4×나 1.8×를 곱해 총합을
> 만들어내지 말 것 — 두 계수 모두 근거가 없다.**

> **("4–6pJ/bit가 SoC측 PHY를 포함하는가" — 2026-07 외부 조사: 해소되지 않았고, 왜
> 해소될 수 없는지가 소득이다.)** 문헌의 관례가 실제로 갈린다:
> - **DRAM 디바이스측 관례(주류).** 학계 pJ/bit 계보는 Micron의 DRAM *디바이스* 전력
>   계산기다(Malladi, ISCA 2012). NVIDIA FGDRAM(MICRO 2017)은 HBM2를 3.92pJ/bit로 놓고
>   데이터패스를 "row-buffer에서 GPU의 핀까지"로 못박는다 — **호스트측 PHY는 경계 밖.**
> - **양측 관례(소수, 명시적).** PIM-AI(arXiv:2411.17309)는 "SoC 인터페이스와 메모리측
>   인터페이스를 모두 포함해 **2× 계수**"라고 쓴다. **그 절대값은 기각한다**(같은 표가
>   LPDDR5→5X를 2.00× 개선으로 함의하는데 실제는 ~20–24%). **관례가 존재한다는 증거로만
>   인용하고 계수로는 쓰지 않는다.**
> - **실물 참고.** NVIDIA Grace: "up to 500 GB/s ... in only about 16W" → ≈**3.4–4.6
>   pJ/bit** [EST, external — "up to"와 "about"의 밴드를 그대로 유지]. **NVIDIA는
>   "memory subsystem"이라고만 쓰고 분해하지 않는다. 범위를 추정하지 않는다.**
>
> **판정 불가의 근본 이유: §4 자신의 4–6pJ/bit에 출처가 없다.** 계보 없는 숫자가 어느
> 관례를 물려받았는지는 **다른 출처의 분해를 읽어서 정할 수 없다.** 따라서 PHY 항은
> **0일 수도(이미 포함) 양수일 수도(미포함) 있고 공개 자료로는 갈리지 않는다.**
> **부정 결과(그 자체가 결과다):** PHY IP 벤더(Synopsys/Cadence/OPENEDGES)는 속도·노드·
> 면적만 공개하고 **pJ/bit를 공개하지 않는다.** 12nm LPDDR4X PHY 논문(arXiv:2503.11654)
> 조차 "12.6mW ... PHY 전체 전력의 약 15%"만 주고 시스템 전력은 후속으로 미룬다. **그
> 15%에서 ~160mW를 역산하는 것은 검토했고 기각했다 — 그게 이 문서가 청소 중인 실패
> 양식이다.** 그러므로 **49–74W는 "DRAM측"으로도 "링크 전체"로도 확정할 수 없고, `≥`가
> 그 불확실성을 계속 진다.** 닫으려면 필요한 단 하나의 데이텀: **BOM 부품의 IDD 표 +
> 그 pJ/bit가 무엇을 포함하는지에 대한 벤더의 명시.**

> **(2026-07) 리텐션은 도출됐고, 범인이 아니다.** 480GB 상주분의 self-refresh 바닥은
> **0.37W(25°C) / 0.59W(45°C) / 1.37W(65°C — 이상 typ)**, 85°C 최악 PVT에서 **4.80W**다.
> **15W를 넘지 않는다.** *"상주 박스라 리텐션 때문에 15W 트래블 모드가 불가능"은 틀렸다 —
> 그렇게 쓰지 말 것.* (Micron MT62F512M32D1 Table 16, 채널당=1GB:
> `0.17mA×1.8V + 0.42mA×1.05V + 0.01mA×0.9V + 0.03mA×0.5V = 0.771mW/GB × 480 = 0.37W`.
> **25/45/65°C는 typ, 85°C는 최악 PVT 보증 MAX — 서로 다른 통계라 한 밴드로 읽지 말 것**
> (데이터시트 Note 1). BOM 부품이 아니라 동급 오토모티브 부품의 **클래스 값**이다.)
> [EST, external]
>
> **다만 eco(~30W)/travel(15W)은 여전히 [미검증]이다 — 리텐션이 아니라 대기 전력 때문.**
> 이번 조사에서 나온 "21.2W 대기 바닥"은 **기각됐다**: **채널당** IDD2N에 **GB 용량**을
> 곱해 480채널(=7680-bit 버스)을 모델링했는데, v3-proto는 1280-bit = **80채널**
> (6GB/채널)이다. 바로잡으면 3.53–5.20W이나, 채널당 6GB는 다중 랭크를 함의하고 비선택
> 랭크도 대기 전류를 먹으므로 그건 **하한**이다. 정직한 상태: **~3.5–21.2W 사이, 미해결**
> — BOM 부품의 실제 IDD 표가 있어야 닫힌다. *(이 6× 오류는 폐기된 25–40W와 같은 유형이
> 축만 바꾼 것이다: 그때는 대역폭, 이번엔 채널 수. 6×로 부풀린 값을 근거로 공개 주장을
> 철회했다면 틀린 숫자를 문서에 굳혔을 것이다 — UNVERIFIED를 WITHDRAWN으로 승격시키지
> 않은 이유다.)*
>
> **부수 확인:** `clk_throttle`은 eco를 만들 수 없다. 자체 예산(~4–5×) 안에서 공짜인
> 이유가 **DRAM 트래픽을 전혀 안 움직이기 때문**이라(`src/clk_throttle.v:14-21`) 지배
> 레일을 못 깎는다. ~30W엔 div≈7–13, 15W엔 div≈49–61이 필요하다 — 공짜 손잡이가 아니라
> 처리량 희생이다. 그리고 `grep -rEi "self[-_ ]?refresh|power[-_ ]?down" src/` = **0 히트**
> (PHY/벤더 기능이라고 `LOW_POWER.md:325`가 이미 인정).

- **외부 전원 필수**: DC 19V 또는 USB-PD (v3면 100–140W로 충분). **PC의 USB-C
  포트는 7.5–15W 소싱이라 불가** — 데이터(USB4)와 전력은 분리 계통.
  옵션: 15W 버스파워 "여행 모드"(스로틀, 한 자릿수 tok/s).
- VRM ~12상: VCORE / VDDQ(LPDDR — v3의 최대 레일) / 보조 분담. 다상 병렬로
  저전압·대전류(레일당 수십 A)의 발열 분산 + 리플 상쇄 + 부하 과도응답.

## 5. 패키징/보드 — 소형화의 열쇠

- **1024-bit 버스를 SoC 패키지 서브스트레이트 안에서 완결** (LPDDR5X 32GB ×16을
  다이 주위에 온-서브스트레이트 실장 — Apple M-Ultra가 양산 실증한 관행).
  광폭 고속 배선이 PCB로 나오지 않으므로 **보드는 8층 · ~120×80mm**로 충분
  (v2의 NAND 뱅크 32팩 + 150×105mm 대비 면적 −40%).
- SoC 패키지 ~80×62mm; BGA ~1,400볼(전원 + PCIe x4 + USB만 — 1024-bit 미포함).
- 뒷면: M.2 2230 + VRM 일부. 방열: 베이퍼챔버 + 저소음 팬 (§4 플로어 ≥50–78W [EST]급
  — 구 40–60W급 표기는 폐기).
- 이 패키징(온-서브스트레이트 16팩)이 **프로젝트 최대 난이도의 하드웨어 항목** —
  관행은 실증돼 있으나 우리가 아직 안 한 일 [PENDING].

## 5a. RTL ↔ SKU 매핑 (2026-07-10, RESIDENT 파라미터 랜딩)

메모리-시스템 RTL은 **한 벌로 세 구성**을 섬긴다 — 이름 일부는 스트리밍
시절의 유물이지만 기능은 티어-불문이며, 상주 구성은 `RESIDENT=1` 파라미터로
RTL 수준에서 검증된다 (`make resident`, 19 tests: 양 모드 DUT 대조 —
RESIDENT=1에서 expert 리필이 ddr5_xbar의 태그된 읽기(TAG_EFILL)로 서빙되고
flash 경로는 시뮬 불변식 `$fatal`로 발화 불가 증명; RESIDENT=0은 기존과 배선
동일 — 상수 폴딩 삼항 + 타이오프 generate, **yosys 순차 등가검증으로 형식
증명** `make resident-equiv`).

| RTL 블록 | rung-① 데모 (스트리밍) | **상주 주 SKU (RESIDENT=1)** | 하이브리드 SKU |
|---|---|---|---|
| `ddr5_xbar` | DDR4/5 fast-tier 팹릭 | **LPDDR5X 팹릭** (이름은 유물, 기능은 티어-불문 크로스바) | LPDDR5X 팹릭 |
| `weight_loader_q4k` | 스트림/캐시 타일 페치 | DRAM 타일 페치 | 양쪽 |
| `expert_cache_pf/ctrl` | h-캐시 본연 | 리필=DDR 읽기 (`glm_q4k_system.v` §10 FSM), hit/miss 의미 불변 | h-캐시 본연 |
| `flash_xbar` + FLASH 채널 | NVMe expert 스트림 | **KV NVMe-스필 전용** (expert 클래스 발화 불가) | expert 스트림 복원 |
| `boot_loader` | 〃 | **NVMe→DRAM 70초 부팅** (형식 증명 완료) | 〃 |
| `kv_cache_pager` | KV 링 + flash 오버플로 | KV 링 + **NVMe 스필** ("무조건 캐싱" 결정의 하드웨어) | 〃 |
| `em_/gn_` 포트 (GDDR6 스텁) | 유휴 | 유휴 — **구세대 컨셉 유물, 테이프아웃 시 제거 후보** | 유휴 |

## 5b. 공급망 노트 — 32GB LPDDR5X 패키지 의존성 (2026-07-11 유통 실사 반영)

512GB = **32GB 패키지 ×16**은 삼성 10.7Gbps LPDDR5X(2024-04 발표, 하반기 양산;
8단 32GB 초박형 2024-08 양산)로 실존하며 Apple M4 Max 128GB(4팩)가 실기 증거.
단 모바일 DRAM은 **B2B/NDA 유통**(공개 카탈로그 부재)이라 조달은 벤더 직접
협상 [PENDING]. **유통 실사(2026-07-11, 웹 확인):** 소매/애프터마켓(폰 수리
부품 시장)에서 실구매 가능한 패키지 상한은 **24GB**(SK hynix H58GJ6BV9HX151R,
9.6Gbps, 496-FBGA — Alibaba/AliExpress 재고 확인; 12GB/16GB도 상시), **32GB는
확인 실패** — OEM 완제품 내장 또는 Micron SOCAMM2(패키지당 64GB, 하이퍼스케일러
전용 모듈)로만 존재. 리스크 완충: 32GB 조달 실패 시 24GB×16=384GB로는 상주가
깨지므로(467GB 미달) → **§5c 프로토 설계점(24GB×20)** 또는 하이브리드 SKU(동일
다이, ONFI 패드)로 회귀; 테이프아웃 시점('27±)엔 32GB 커머디티화 + LPDDR6 세대
진입으로 리스크 자연 감소 전망.

## 5c. v3-proto — 소매부품 프로토 설계점 (2026-07-11 결정: 프로토 우선)

**결정.** rung-③ 실행 순서를 "양산 SKU 먼저"에서 **"소매 조달 가능한 프로토
먼저"로 재배열.** §5b 실사로 32GB NDA 조달과 온-서브스트레이트 16팩(§5 —
프로젝트 최대 난이도 항목)이 프로토 크리티컬 패스에서 빠지고, 실리콘만 나오면
나머지는 즉시 조립 가능해진다.

| | **v3-proto (프로토, 소매 조달)** | v3-volume (양산 SKU) |
|---|---|---|
| DRAM | **24GB ×20 (SK hynix 9.6Gbps, 소매)** | 32GB ×16 (벤더 협상) |
| 용량 | 480GB (467GB 모델 + ~13GB KV 링; KV는 NVMe-스필이 흡수) | 512GB |
| 버스 | **1280-bit** (20팩 × 64-bit) | 1024-bit (같은 다이, 4채널그룹 미배선) |
| 대역폭 | ~1.54TB/s → **~110 tok/s [EST]** (§3 전제: **전용 12,732 lane** @490MHz — 실측 0.62 B/param + 순차 국면 전용 풀. 어레이가 이보다 작으면 대역폭이 아니라 어레이가 병목) | ~1.1–1.37TB/s → ~80–100 [EST] (전용 ~9,412 lane) |
| 실장 | **PCB-HDI 직실장** (폰 관행: 8.5–9.6Gbps를 PCB 수 cm 라우팅), **12층**(신호6·GND4·전원2 — SoC escape ~2,800신호 ÷ 층당 ~460 = 신호층 6 필요; 10층은 전원 평면 1장으로 PI 미달), 보드 ~130×110mm | 온-서브스트레이트 16팩, 보드 120×80mm |
| 다이 | 1280-bit PHY beachfront ~75–85mm → **패드-리미티드 ~480mm²** [EST] — 12–16nm에서 원가 ~$40–60, BOM 허용 범위 | 같은 다이 (수퍼셋) |

- **다이는 하나다**: 1280-bit로 파두면 1024-bit 구성을 포용(남는 채널 미배선)
  — 프로토와 양산 SKU가 같은 실리콘, 마스크 재사용.
- **프로토 SoC 패키지 볼아웃 [EST]**: 버스가 보드로 나오므로 신호 ~2,800
  (DQ 1,280 + WCK/RDQS/DMI ~800 + CA/CK/CS ~800) + 전원/GND → **0.8mm 피치
  ~4,300볼, ~50×50mm** — 서버 CPU급(AMD SP5 6,096볼) 관행 안. §5의 ~1,400볼은
  버스가 패키지 안에서 끝나는 양산형 수치로, 프로토엔 적용되지 않음.
- **정직한 한계**: 이 피벗이 없애는 것은 *패키징·조달* 리스크다. **SoC
  테이프아웃 자체(LPDDR5X PHY IP 라이선스 + 12–16nm NRE)는 프로토라고
  사라지지 않는다** — rung-③의 진짜 관문은 여전히 실리콘 [PENDING].
- 12GB×40(2-rank 1280-bit, 480GB) 극단은 DRAM 최저가지만 40팩 실장이 보드를
  서버-미니급으로 키우고 그레이마켓 QC 리스크가 팩 수에 비례 — 기각, 근거 박제.
- 24GB 소매 파트가 9.6Gbps라 기존 v3 계산(8.5Gbps, 1.1TB/s)보다 **대역폭이
  오히려 상향**된다(1024-bit 기준으로도 1.23TB/s ≈ ~89 tok/s [EST]).
- **라우터빌리티 스터디로 검증** ([`hw/board_study/`](../hw/board_study/), 2026-07-11):
  "12층 / 130×110mm" 주장이 손계산에서 **검증 가능한 아티팩트**로 격상됨.
  (a) `escape_analysis.py` — via-in-pad HDI ring-cut 모델로 SoC 1280-bit escape가
  **신호 ~4층 필요 → 12층의 6층 예산 안에 여유 2층으로 PASS** (폰-AP 캘리브레이션
  ~680신호 → 5층 vs 실제 ~6: 모델은 대략 보정됨 ±1–2층, 보수적 상한 아님. 4층이
  1–2층 낙관이어도 ~4–6이 6 안에 들어 여유 2층이 불확실성을 흡수 — 라우팅 보드보다
  마진은 빡빡). (b) `gen_gerbers.py` — 실치수
  RS-274X 거버 + 배치 DRC: **20 DRAM + SoC가 130×110mm에 0 courtyard 겹침 · 1mm+
  엣지 이격 · 43% 면적으로 물리적 적합**. 첫 실행 DRC가 실오류를 잡음(코너에서
  15mm 패키지 4개가 SoC 50mm 변에 안 들어감 → 좌우 열을 코너 레인으로 재배치).
  스코프: 해석적 채널 카운트 + 배치/아웃라인 거버(라우팅 카퍼 아님); fab-ready는
  실 SoC 볼아웃 + SI 검증(임피던스·길이매칭) 필요 [PENDING].
- **전력 [EST]**: 메모리-바운드 설계라 전력은 대역폭에 비례 — §4 자체 계수(4–6pJ/bit)로
  1.54TB/s의 DRAM 레일 **49–74W** + SoC **미검증** = **≥64–99W [EST, 재도출] 플로어**
  (v3-volume은 ≥50–78W, §4). 구 "~35–55W + SoC 15–25W = ~50–80W" 계산은 폐기된 항들의
  ×1.4 재조합이라 함께 폐기(§4 재도출 블록). 냉각 방식(베이퍼챔버+팬)은 유지되나,
  "동일 급" 주장은 플로어 상향으로 재검토 대상.

## 6. BOM [EST, 양산 가정]

| 품목 | 개략가 |
|---|---|
| LPDDR5X 32GB ×16 | ~$1,500–1,900 (지배 항목) |
| M.2 NVMe 1TB | ~$60 |
| WPU SoC (양산단가; NRE 별도) | ~$50–150 |
| 패키지(서브스트레이트+조립) | ~$120–220 |
| PCB 8층 + VRM | ~$50–90 |
| 방열/케이스/전원 | ~$60–120 |
| **합계** | **~$1.8–2.4k** → 소비자가 ~$3.5–5k대 [EST] |

## 7. 경쟁 포지셔닝 — "체급"을 정확히

**사용자당 출력 속도, 같은 브래킷 비교** (API 수치는 시점/부하에 따라 변동):

| | 사용자당 tok/s | 비고 |
|---|---|---|
| 사람 읽는 속도 | ~7–12 | 기준선 |
| Claude Opus급 API | ~25–60 | 네트워크+큐 지연 별도 |
| Gemini Pro급 API | ~40–90 | 〃 |
| Mac Studio M3 Ultra 512GB ($10k+) | ~15–25 (이 급 모델, llama.cpp) | 현존 동브래킷 최강 |
| **WPU v3 [실측-입력 EST]** | **≈80 — 결정론** (GLM-5.2 MTP 공표 수준이면 ~95) | 첫 토큰 지연 無, 피크타임 저하 無 |

- **Groq/Cerebras(500+ tok/s)는 다른 종목**: 가중치를 SRAM에 상주(칩당 ~230MB)
  → 70B급에 칩 수백 개, 랙당 $수백만, 수천 사용자 분할상환. 467GB 모델은
  싱글박스 자체가 불가. 우리 브래킷(1인·1박스·풀 프런티어 모델·<$5k)에서
  500 tok/s는 물리적으로 성립하지 않음(467GB SRAM 불가; HBM 3TB/s는 $수만).
- 피치: **"프런티어급 오픈 모델을 Opus/Gemini-Pro 속도로, 오프라인에서, 구독
  없이"** — 동브래킷 최강(Mac Studio) 대비 ~3–5× 속도를 절반 가격에 (80 vs 15–25 tok/s
  산술 = 3.2–5.3×).

## 8. 기각된 대안들 (근거 박제)

- **CXL 512GB**: 링크당 실효 ~50GB/s(x16 Gen5) = 문헌의 PCIe 오프로딩 벽 →
  ~4–5 tok/s, $/GB ~30×, CXL 루트는 서버급 실리콘. 용량은 풀고 대역폭은 못 풂.
- **512GB DDR5 DIMM 상주**: ~350–540GB/s → ~25–35 tok/s. 용량을 느린 매체에
  강제. 서버 폼팩터.
- **256GB 하이브리드 (v1 고정안)**: min(NAND,DRAM) 교정 후 512-bit ~42 tok/s;
  1024-bit ~84는 GLM h≥0.75 베팅(빗나가면 ~45) — 업사이드 SKU로만 보류.

## 9. 성능/비용 레버 감사 (2026-07-10 결정)

v3 기준(≈80 tok/s, §2 재도출 — 구 ~71 표기는 15.4GB/tok 상수와 함께 폐기)에서
"더 빠르고 더 싸게"의 레버 전수 감사 결과:

- **✅ 채택 — 적응형 스펙체인 (RTL 반영 완료 — LANDED, commit 6c5332f).** 측정
  U(K) (GLM-4.5-Air 실측, 워크로드 평균)로 K-스윕:

  | K | U (GLM-Air 실측) | tok/s @r=0.8 | @r=0.85 | @r=0.9 |
  |---|---|---|---|---|
  | 1 | 1.00 | 78 | 80 | 82 |
  | 3 | 2.16 | 78 | 84 | 91 |
  | 4 | 2.65 | 76 | 84 | 93 |
  | 5 | 3.11 | 74 | 83 | **94** |
  | 8 | 4.27 | 67 | 79 | 95 |

  결론: **최적 깊이는 수락률 r의 함수** — r=0.8이면 K=2–3, r=0.9면 K=4–5에서
  고원(~94, K>5 무이득 → 적응 범위 K∈[1..5]로 하드웨어 단순화). 고정 K가
  아니라 **런타임 수락률 관찰 → 깊이 조절(적응형)**이 설계 정답. spec==greedy 속성이 모든 K에서 성립하므로 적응은
  **출력 불변**(스루풋만 변화) — 비트 정확 무트와 무충돌.
  **(updated 2026-07: LANDED — commit 6c5332f.** spec_decode_seq에 ADAPT
  파라미터(기본 0 — 기존 소비자는 yosys 순차 등가 PROVEN 무변경) + k_cur 포트 +
  pass_* 탭; 신규 `src/spec_depth_adapt.v` 적응 정책(포화 스트릭); 임의 깊이
  스케줄에서 spec==greedy — 구성상 출력 불변. 게이트: spec_depth_adapt 31,522 ·
  spec_decode_seq(K>1) 3,702 (K=1/2/3/4/6/8) · K=1 정확 621 · spec_chain_top 4/4
  (신규 DRAFT_K=4 엔진 포함) · spec_batched_top 8/8 · spec_decode_top 18/18 ·
  `make spec-adapt` 게이트 신설.)
  **(갱신 2026-07-10: job B 완료 — r 실측됨.** GLM-4.5-Air 자체 MTP를 vLLM으로
  스윕(`tools/modal_glm_accept.py`): r₁=0.87, 위치별 0.87/0.60/0.32/0.13/0.04,
  A_eff ~2.9 고원 → 메모리-바운드 박스의 최적 깊이 **K=1–2**, 설계점 **≈ 80
  tok/s [실측-입력 EST]** — §2 "갱신 2" · [`H_MEASUREMENT.md`](H_MEASUREMENT.md)
  3차 측정. K≤5 적응 범위는 GLM-5.2의 더 깊게 훈련된 MTP(공표 4+ 수락)용
  보험으로 유지. 남은 정밀화는 GLM-5.2 플래그십 자체 r/U 확인 실측뿐.)
- **📋 계획 전용 — LPDDR6.** 같은 16팩·같은 폭에서 BW ~1.5× → ~105 tok/s.
  테이프아웃 시점('27±)에 성숙 예상 — PHY IP 교체만으로 얻는 세대 레버라
  **능동 작업 없음, 로드맵 항목으로만 유지.** (적응형 스펙체인과 합산 시
  ~130+ [EST].)
- ⏸ 보류: 핫셋 HBM 분리(+$300–500, 2.5D 패키징 점프 — LPDDR6가 더 싸게 동일
  효과), LPDDR-PIM(생태계 미성숙, 관찰만).
- ✗ 기각: expert 저비트화(비트 정확 무트 파괴), 로직 칩렛 분리(NRE↓가 패키징↑로
  상쇄), 메모리 하위 빈(성능↓).

## 10. 열린 것 [PENDING/EST]

**v3-proto 크리티컬 패스(§5c, 프로토-우선 결정 이후):** SoC 테이프아웃
(LPDDR5X PHY IP 라이선스 + 12–16nm NRE — 유일한 진짜 관문) · 12층 HDI 보드 실설계 · 24GB 소매 물량 스크리닝(그레이마켓 QC) — 이후 양산 SKU로: 32GB 벤더
협상 · 온-서브스트레이트 16팩 패키징 실설계.
그 외: GLM-5.2 플래그십 r/U 확인 실측(GLM-4.5-Air 실측은 완료 —
[`H_MEASUREMENT.md`](H_MEASUREMENT.md); 플래그십 8×H100 ~$50–100) ·
보드 브링업(rung ①, 클라우드 FPGA 후보) · 용량 천장
(차세대 >512GB 모델은 하이브리드 SKU로 회귀). *(Q8_0 GGUF 교차검증 확장은 완료 —
Q4_K/Q6_K/Q8_0 전부 실 GGUF 비트단위 PASSED, [`GGUF_CROSSCHECK.md`](GGUF_CROSSCHECK.md).)*
