# expected_test_counts.txt -- EXACT per-gate test-count manifest (enforced by
#   tools/check_test_counts.sh, wired as `make release-gate-strict`).
#
# Each gate recipe greps `ALL [0-9]+ TESTS PASSED` -- a wildcard that still passes if a
# testbench silently ran FEWER tests than intended (a reduced loop bound, a dropped
# `ifdef`, a missing build flag such as -DSPEC_FULL).  This manifest pins the EXACT count
# each gate must print; the checker fails on any mismatch or a gate that never printed.
#
# FORMAT:  NAME  c1 [c2 ...]
#   NAME  = the exact string inside the gate's `[...]` banner prefix (no spaces).  A unit
#           exercised under two configs prints two DISTINCT banner names when the recipe
#           tags them (e.g. moe_router_q4k vs moe_router_q4k(256/top-8)) -- those are
#           separate lines here.
#   c1..  = the expected test count(s).  A few names print MORE THAN ONCE with the SAME
#           banner (the same recipe run twice, e.g. inside two meta-targets) -- for those
#           the full MULTISET of counts is pinned, and the checker compares them as
#           histograms so a drop/add of any single run, or a change to any one count, fails.
#
# MAINTENANCE: when you add or remove test cases (so a gate's printed count changes), or
#   add/remove a gate invocation, update the matching line here.  That is the intended
#   friction: a test count is a contract, and a silent change to it is a regression.
#
# Captured from a GREEN `make release-gate` (2026-07, exit 0, "release-gate: ALL gates
#   passed").  Regenerated with the checker's own banner parser so the manifest and checker
#   agree by construction (distinct parenthetical config-variant names kept separate).
#   Non-"ALL N TESTS PASSED" gates (synth-glm cell counts, formal unsat, cdc, the
#   structural-equiv gates and the must-FAIL injection steps) are intentionally absent --
#   they do not emit this banner and are checked by their own recipes.  Every gate that
#   DOES emit the banner must be pinned here: the checker now FAILS on any countable
#   banner without a manifest line (2026-07-22 -- dsa-sparse-correct, boot_loader_manifest
#   and host-test had banner formats the parser could not see, and ran count-unchecked;
#   their banners are normalized and pinned below).

kda_onorm 1152
kda_onorm_gen 300
kda_gate 1224
kda_gate_gen 5
kda_conv 1280
kda_conv_gen 400
fp32_ieee 10000
kda_recur(RSQRT) 5184
kda_recur(EXACT) 5184
kda_gen 80
glm53f-ref 15
glm53f-config-guard 8
axi_boot_writer 403
axi_master_dma 7
boot_loader 9240
boot_loader_manifest 3712
cdc_async_fifo 309
cdc_protocol_ctx 11
clk_en_ctrl 12
clk_gate_cluster 16
clk_throttle 4
ddr4_mig_shim 16
ddr5_xbar 3073
dsa-sparse-correct(DSA_REAL_IDX=0) 4
dsa-sparse-correct(DSA_REAL_IDX=1) 4
dsa_indexer 200
ecc_mem_wrap 22290
ecc_secded 233920
expert_cache_ctrl 8660
expert_cache_pf 623
expert_predictor 20
flash_xbar 2049
glm_act 5232
glm_decoder_block 10
glm_fp_pipe 50275
glm_matmul 42
glm_matmul_mixed 32 32 32
glm_matmul_pipe 480
glm_matmul_q4k 160 160
glm_matmul_q4k(HDR:0) 160
glm_matmul_q4k(HDR:1) 160
glm_matmul_q4k(HDR:1L) 160
glm_matmul_q4k(K=6144) 120
glm_model 5
glm_model_q4k_full 1155
glm_model_q4k_full(ACT_HW=1) 1155
glm_model_q4k_full(spec) 99
glm_model_q4k_pem(PE_M=2) 4233
glm_q4k_intra_batch_verify 9
glm_q4k_spec_system(spec==greedy,K=1-3) 31
glm_q4k_system(LOOPBACK=1==ref) 5
glm_q4k_system(LOOPBACK_FW=1==ref) 5
glm_q4k_system(LOOPBACK_REST=1==ref) 10
glm_q4k_system(RESIDENT) 19
glm_q4k_system(SELF_KV=1,kv-roundtrip) 4
glm_softmax 1152
glm_softmax(LEN=2048) 32896
host-test 32
icg_cell 6
kv_cache_pager 73
kv_cache_pager(ECC) 42
kv_cache_pager(NSEQ>1) 84
kv_cache_pager(RESIDENT=S_MAX) 69
kv_ecc_ring 8
l3_e2e 11
l3_hash_mirror 704
mbist_ctrl 12
mbist_ctrl_2p 11
mla_attn 7
mla_attn_q4k_sparse_perrow 53
moe_router 896
moe_router_q4k 40 40
moe_router_q4k(256/top-8) 40
mtp_head 5
packer_rtl_crosscheck 1608
q4k_prim 18 18
q6k_prim 91220 91220 91220
q8_0_prim 10816 10816 10816
reset_sync 7
rmsnorm_unit 55
rmsnorm_unit(LEN=6144) 55
rope_interleave_unit 2512
rope_interleave_unit(ROT_DIM=64) 2512
sampler 44
spec_batched_top 8
spec_chain_top 4
spec_decode_seq 621
spec_decode_seq(K>1) 3702
spec_decode_top 18
spec_depth_adapt 31522 31522
spi_boot 51
swiglu_expert 20
swiglu_expert_q4k 240 240
swiglu_expert_q4k(CLAMP) 240 240
swiglu_expert_q4k(INTER=2048) 256
topk_select 442
uart_host_bridge 15
weight_decomp 34368 34368
weight_ecc 67
weight_loader_lanes(PE_N=32) 1536
weight_loader_q4k 160 160 160
weight_loader_q4k_mixed 192 192 192
